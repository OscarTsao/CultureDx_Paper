% fig_pipeline_tikz.tex — v3: unified agent-style names + intermediate-output arrow labels (maps to Table 3.1)
\documentclass[border=3pt]{standalone}
\usepackage{tikz}
\usetikzlibrary{positioning, arrows.meta}
\usepackage{helvet}
\renewcommand{\familydefault}{\sfdefault}

\definecolor{procFill}{HTML}{F1EFE8}
\definecolor{procDraw}{HTML}{888780}
\definecolor{procText}{HTML}{2C2C2A}
\definecolor{agentFill}{HTML}{FDF0D5}
\definecolor{agentDraw}{HTML}{C9820C}
\definecolor{agentText}{HTML}{8A5A00}
\definecolor{ragFill}{HTML}{FBE4E4}
\definecolor{ragDraw}{HTML}{B23A48}
\definecolor{ragText}{HTML}{7A1F2B}
\definecolor{logicFill}{HTML}{F0E6F6}
\definecolor{logicDraw}{HTML}{7B4FA0}
\definecolor{logicText}{HTML}{4A2E63}
\definecolor{decFill}{HTML}{E1F5EE}
\definecolor{decDraw}{HTML}{0F6E56}
\definecolor{decText}{HTML}{085041}
\definecolor{audFill}{HTML}{E6F1FB}
\definecolor{audDraw}{HTML}{185FA5}
\definecolor{audText}{HTML}{0C447C}
\definecolor{lblText}{HTML}{55524A}

\begin{document}
\begin{tikzpicture}[
  font=\small,
  box/.style={rectangle, rounded corners=3pt, draw, line width=0.5pt,
              align=center, inner sep=4pt, minimum height=11mm, minimum width=18mm},
  proc/.style={box, fill=procFill, draw=procDraw, text=procText},
  agent/.style={box, fill=agentFill, draw=agentDraw, text=agentText},
  ragbox/.style={box, fill=ragFill, draw=ragDraw, text=ragText},
  logic/.style={box, fill=logicFill, draw=logicDraw, text=logicText},
  dec/.style={box,  fill=decFill,  draw=decDraw,  text=decText},
  aud/.style={box,  fill=audFill,  draw=audDraw,  text=audText},
  arr/.style={-{Stealth[length=4pt,width=4pt]}, line width=0.5pt, draw=procDraw},
  lbl/.style={font=\scriptsize\itshape, text=lblText},
]

% ---- Row 1: selection path ----
\node[proc]   (tx)  at (0,0)     {Transcript};
\node[ragbox] (rag) at (3.6,0)   {Similar Cases\\Retriever\\[-1pt]{\scriptsize 1 case / disorder}};
\node[agent] (dx)  at (7.4,0)   {Diagnostician\\[-1pt]{\scriptsize 5 candidates}};
\node[dec]  (pri) at (12.4,0)  {Committed primary\\[-1pt]{\scriptsize + comorbid}};

% ---- Row 2: audit branch ----
\node[agent] (chk)   at (3.6,-2.9)  {Criterion\\Checkers\\[-1pt]{\scriptsize Per-disorder ICD-10}};
\node[logic] (logic) at (7.4,-2.9)  {Compatibility\\Auditor\\[-1pt]{\scriptsize Count thresholds}};
\node[aud]  (audit) at (12.4,-2.9) {Audit record\\[-1pt]{\scriptsize criteria \& why}};

% ---- Selection-path arrows (with intermediate-output labels, matches Table 3.1 provenance) ----
\draw[arr] (tx)  -- node[lbl, above] {transcript} (rag);
\draw[arr] (rag) -- node[lbl, above] {similar cases} (dx);
\draw[arr] (dx)  -- node[lbl, above, text=procText, font=\scriptsize] {Direct-Answer} (pri);

% ---- Branch: candidates feed the checkers ----
\draw[arr] (dx.south) -- ++(0,-0.9) -| node[lbl, pos=0.28, above] {ranked candidates} (chk.north);

% ---- Audit-branch arrows ----
\draw[arr] (chk)   -- node[lbl, above] {criterion states} (logic);
\draw[arr] (logic) -- node[lbl, above] {logic-confirmed set} (audit);

\end{tikzpicture}
\end{document}
