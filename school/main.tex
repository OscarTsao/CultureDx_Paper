\documentclass[12pt,a4paper,oneside]{report}
\usepackage{fontspec}
\usepackage{xeCJK}
\setmainfont{Liberation Serif}
\setsansfont{Nimbus Sans}
\setmonofont{Liberation Mono}
\setCJKmainfont{Noto Serif CJK TC}
\setCJKsansfont{Noto Sans CJK TC}
\setCJKmonofont{Noto Sans Mono CJK TC}
\usepackage{amsmath,amssymb,graphicx}
\graphicspath{{figures/}{school/}{./}{../figures/}}
\makeatletter
\providecommand\input@path{}%
\g@addto@macro\input@path{{figures/}{school/}{../figures/}}%
\makeatother
\usepackage{booktabs,array,tabularx,longtable,multirow,adjustbox,float}
\usepackage{enumitem,setspace}
% CCU 學位論文格式規範（90 年版）版面：上 2.5 / 下 2.5 / 左 3 / 右 2 cm；
% footskip=1cm 使頁碼落於版面底端上方 1.5 cm 處。若系所模板另有規定，以模板為準。
\usepackage[top=2.5cm,bottom=2.5cm,left=3cm,right=2cm,footskip=1cm]{geometry}
\usepackage[numbers,sort&compress]{natbib}
\usepackage{xcolor,microtype,url}
\usepackage[pages=all,scale=1,angle=0,opacity=1.0]{background}
\backgroundsetup{contents={\includegraphics[width=0.5\textwidth]{watermark.jpg}}}
\emergencystretch=3em
\usepackage{fancyhdr,titlesec,tocloft}
\usepackage[hidelinks]{hyperref}
\hypersetup{
  pdftitle={HiED: A Hybrid, Evidence-grounded Multi-Agent System for Differential Diagnosis of Common Mental Disorders in the Taiwanese Context},
  pdfauthor={Yu-Ning Tsao / 曹喻寧}
}
\usepackage[nameinlink,noabbrev]{cleveref}
\onehalfspacing
\setlength{\parskip}{0.35em}
\setlength{\parindent}{2em}
\pagestyle{fancy}
\fancyhf{}
\fancyfoot[C]{\thepage} % CCU 規範：頁碼置於版面底端置中；保守格式：無 running header
\renewcommand{\headrulewidth}{0pt}
\setlength{\headheight}{15pt}
\titleformat{\chapter}[display]{\raggedright\bfseries\LARGE}{Chapter \thechapter}{0.5em}{}
\newcommand{\placeholder}[1]{\textcolor{red!70!black}{[#1]}}
\newcommand{\ThesisFigure}[4][0.88\textwidth]{\begin{figure}[htbp]\centering
\IfFileExists{#2}{\includegraphics[width=#1]{#2}}{\fbox{\parbox[c][5.2cm][c]{0.84\textwidth}{\centering Figure placeholder for \texttt{\detokenize{#2}}\\[0.4em]The compiled thesis inserts the figure artifact.}}}
\caption{#3}\label{#4}\end{figure}}

% Guard 1: set \showcommentstrue for mentor-review builds; switch to \showcommentsfalse only for clean defense exports.
\newif\ifshowcomments \showcommentstrue
\newcommand{\ct}[1]{\ifshowcomments\textcolor{red}{{[CT: #1]}}\fi}
\newcommand{\wl}[1]{\ifshowcomments\textcolor{blue}{{[WL: #1]}}\fi}

\begin{document}
\begin{titlepage}
\thispagestyle{empty}
\centering

\vspace*{2.0cm}

{\Large 國立中正大學資訊工程研究所\par}
\vspace{0.45cm}
{\Large 碩士論文\par}

\vspace{2.2cm}

{\LARGE\bfseries
HiED: A Hybrid, Evidence-grounded\\[3pt]
Multi-Agent System for Differential Diagnosis of\\[3pt]
Common Mental Disorders in the Taiwanese Context\par}

\vspace{2.6cm}

{\large
\begin{tabular}{@{}r@{\quad}l@{\quad}l@{}}
指導教授 & 林維暘 & 博士\\[0.25cm]
         & 蔡佳玲 & 博士
\end{tabular}\par
}

\vspace{0.9cm}

{\large 研究生\quad 曹喻寧\par}

\vfill

{\large 中華民國一百一十五年七月\par}

\vspace*{1.6cm}
\end{titlepage}
\pagenumbering{roman}
\chapter*{中文摘要}
\addcontentsline{toc}{chapter}{中文摘要}
初診精神科晤談逐字稿中的鑑別診斷，本質上是一種在證據不完整條件下進行的比較式決策。多個疾病診斷可能同時與同一份逐字稿相容，但最終通常只能有一個被判定為主診斷。然而，用以區分病程、發病時序、排除病因與共病關係等關鍵鑑別資訊，未必會在單次初診晤談中被完整引出。大型語言模型雖然能流暢地閱讀並理解這類逐字稿，近期也已有大量研究嘗試將其應用於精神科診斷任務，但語言理解的流暢性並不等同於可靠的鑑別診斷能力。

既有評測呈現出一致的落差：模型在粗粒度分類任務上往往表現良好，但一旦進入細粒度的疾病層級鑑別診斷，準確率便會大幅下降。這個落差促使本研究提出一套可稽核的系統架構，用以明確定位錯誤發生於診斷流程中的哪一個階段。模型可能生成一個合理且受到診斷準則支持的候選疾病，卻仍未能將其正確選為主診斷。候選診斷生成、依準則驗證，以及主診斷選擇三者之間的差異，通常會被單一準確率指標所掩蓋，使研究者難以判斷系統究竟在哪個環節失敗。

為了使這些不同的失敗階段能夠被分別觀察，並檢驗主診斷選擇是否構成主要瓶頸，本研究提出 HiED——一套面向中文精神科初診對話逐字稿的鑑別診斷框架。HiED 將候選診斷偵測、ICD-10 逐準則驗證，以及主診斷選擇保留為可分別分析的階段，而非將整個流程簡化為單一分類事件。同時，系統會在每次預測時輸出逐準則的稽核軌跡，使研究者能夠檢視診斷依據並定位系統失敗的位置。HiED 並非已完成臨床驗證的自主診斷工具，而是一項用於分析與檢驗逐字稿式精神科診斷失敗模式的研究工具。

因此，本論文有兩項主要貢獻：其一，提出一套可稽核的失敗定位框架，使上述各診斷階段得以分別觀察；其二，提供可重現的證據，指出在本研究評估的 HiED 設定與合成逐字稿基準評測中，主診斷選擇構成主要的失敗瓶頸。換言之，在本研究之合成逐字稿設定下，系統的主要限制不僅在於能否生成合理的候選診斷，而更在於能否在多個皆具一定準則支持的候選之間，進行有效比較並判定最適合作為主診斷者。最後需說明，本文題目所稱之臺灣脈絡，指涉系統之設計動機與預期部署場域（繁體中文互動、本地端推論、面向臨床人員之稽核），而非已完成之臺灣真實病人臨床驗證；本文所有量化結果均來自合成中文逐字稿語料，真實病人之臨床驗證為後續工作。

\noindent\textbf{關鍵詞：}大型語言模型、多代理系統、精神科鑑別診斷、臨床決策支援、ICD-10、可稽核人工智慧
\chapter*{Abstract}
\addcontentsline{toc}{chapter}{Abstract}
Differential diagnosis from an initial-consultation psychiatric transcript is a comparative decision under incomplete evidence. Multiple disorders may be compatible with the same transcript, yet only one can typically be judged the primary diagnosis. Key discriminating information---illness course, onset timing, exclusionary causes, and comorbidity relationships---may not be fully elicited within a single initial consultation. Large language models can read such transcripts fluently, and recent research has applied them to psychiatric diagnostic tasks, but linguistic fluency does not equate to reliable differential diagnostic ability.

Existing evaluations show a consistent gap: models perform well on coarse-grained classification, but accuracy drops sharply on fine-grained, disorder-level differential diagnosis. This gap motivates an auditable architecture that locates the stage at which a diagnostic error occurs. A model may generate a plausible disorder supported by diagnostic criteria, yet fail to select it as the primary diagnosis. The distinction among candidate-diagnosis generation, criterion-based verification, and primary-diagnosis selection is usually obscured by a single accuracy metric.

To make these failure stages separately observable and to examine whether primary-diagnosis selection is a major bottleneck, we propose HiED, a differential-diagnosis framework for Chinese psychiatric initial-consultation transcripts. HiED retains candidate detection, ICD-10 criterion verification, and primary selection as separately analyzable stages rather than reducing diagnosis to one classification event. A criterion-level audit trace records evidence and failure locations for each prediction. HiED is a research instrument for analyzing transcript-based diagnostic failures, not a clinically validated autonomous diagnostic tool. The Taiwanese context in the title denotes the intended deployment setting; real-patient validation in Taiwan remains future work.

This thesis makes two main contributions: an auditable failure-localization framework and reproducible evidence of a primary-selection bottleneck in the evaluated HiED configurations under synthetic transcript-only benchmarks. In these settings, the observed limitation lies not only in generating plausible candidates, but in comparing criterion-supported alternatives and selecting the most appropriate primary diagnosis.

\noindent\textbf{Keywords:} Large language models, multi-agent systems, psychiatric differential diagnosis, clinical decision support, ICD-10, auditable AI

\chapter*{Acknowledgements}
\addcontentsline{toc}{chapter}{Acknowledgements}

I am deeply grateful to my advisors, Professor Wei-Yang Lin (林維暘)
and Professor Chia-Ling Tsai (蔡佳玲), for their guidance throughout
this work, their continuous support, and their insightful feedback.
Their complementary perspectives shaped both the direction and the
rigor of this thesis, and it would not have been completed without
them.

I owe special thanks to Dr.\ Yu-Shu Huang (黃玉書), Professor and
Director of the Department of Psychiatry at Linkou Chang Gung Memorial
Hospital, who served as the clinical principal investigator for this
research. Her deep expertise in psychiatric diagnosis and her clinical
guidance shaped the design of the differential-diagnosis framework,
and the protocol for the planned prospective clinical study. I am
sincerely thankful for her generosity with her time and for her
commitment to bridging clinical psychiatry and computational methods.

I thank the clinical team of the Division of Psychosomatic Medicine at
Chang Gung Memorial Hospital for their collaboration on the study
protocol and clinical review.

I am grateful to members of the laboratory for their help and many
helpful discussions.

Finally, I thank my family for their unwavering support and
encouragement.
\chapter*{List of Abbreviations}
\addcontentsline{toc}{chapter}{List of Abbreviations}
\begin{longtable}{|p{3cm}|p{10cm}|}
\hline Abbreviation & Meaning\\ \hline
AI & Artificial intelligence\\
\hline
SC & Self-consistency\\
\hline
CISC & Confidence-informed self-consistency\\
\hline
DA & Direct-Answer\\
\hline
ICD-10 & International Classification of Diseases, Tenth Revision\\
\hline
LLM & Large language model\\
\hline
NtS & Nominate-then-Select\\
\hline
DTV & Diagnose-then-Verify\\
\hline
BGE-M3 & Multilingual embedding/retrieval model (BAAI General Embedding, M3)\\
\hline
FAISS & Facebook AI Similarity Search (vector-index library)\\
\hline
AWQ & Activation-aware Weight Quantization\\
\hline
RAG & Retrieval-augmented generation\\
\hline
TF--IDF & Term frequency--inverse document frequency\\
\hline
vLLM & High-throughput LLM inference and serving framework\\
\hline
\end{longtable}
\clearpage
\tableofcontents
\clearpage
\listoftables
\clearpage
\listoffigures
\clearpage
\pagenumbering{arabic}

\chapter{Introduction}
\label{ch:introduction}

\section{Background and Motivation}

Psychiatric outpatient care in Taiwan is widely accessible, but this access also creates a heavy clinical workload. Under National Health Insurance, patients may visit different levels of medical facilities without referral, and large medical centers may receive more than ten thousand outpatient visits across departments in one day~\citep{lin2020outpatient}. A first psychiatric visit must cover history taking, mental-status observation, risk screening, and an initial diagnostic judgment within limited time. The resulting note or transcript often contains incomplete information about onset, duration, course, medical or substance-related causes, and previous treatment.

The evidence may also be indirect. In Chinese-speaking settings, patients often describe distress through insomnia, fatigue, dizziness, chest tightness, or gastrointestinal symptoms before using explicit mood or anxiety terms~\citep{kleinman1982neurasthenia,ryder2008somatic}. Taiwanese consultations may also mix Mandarin, Taiwanese expressions, and local clinical wording. The same condition can therefore be described in several ways within one visit.

Differential diagnosis remains difficult because common psychiatric disorders share many symptoms. Depression, anxiety, stress-related disorders, obsessive-compulsive disorder, somatic symptom disorders, and sleep disorders may all involve insomnia, fatigue, poor concentration, and impaired daily function. In diagnostic assessment, these symptoms are interpreted through disorder-specific criteria that also consider their number, duration, functional impact, clinical course, and exclusion conditions~\citep{who2019icd10}. A diagnosis may therefore share several symptoms with the transcript without satisfying all of the information required to distinguish it from competing diagnoses. Even when diagnostic criteria are applied, several disorders may remain compatible with the same transcript. Evidence for both depression and anxiety, for example, may reflect comorbidity or overlapping symptoms from one primary condition~\citep{brown2001comorbidity}. Even structured diagnostic studies report limited agreement for some common disorders~\citep{regier2013dsm5}.

These features make first-visit psychiatric diagnosis a problem of comparative bookkeeping under incomplete evidence. The clinician must document a best-supported working diagnosis, keep plausible alternatives visible, and identify which missing information should be collected at follow-up. Time pressure can increase the risk of anchoring or premature closure when several disorders share the same symptoms~\citep{croskerry2003cognitive}.
\section{Problem Definition and Challenges}
\label{sec:problem-definition}

Large language models offer capabilities that are well aligned with psychiatric transcript analysis: they can process free-form dialogue, summarize symptom evidence, retrieve relevant examples, and produce structured outputs. Medical multi-agent systems can further divide the diagnostic task among specialized roles and combine their outputs~\citep{li2024taiwan,raballo2025,sarma2025,tang2024medagents,kim2024mdagents}. However, these capabilities do not by themselves ensure reliable differential diagnosis from a fixed transcript. Under benchmark evaluation, a system may fail because it does not propose a dataset-provided diagnosis, because criterion verification does not retain that diagnosis, or because the diagnosis is proposed but another candidate is selected as primary. HiED studies this narrower technical problem; it does not solve short consultation time, missing history, or clinical uncertainty.\ct{Does our model deal with all these problems?}

We study this question through three diagnostic stages. \emph{Candidate detection} asks whether a benchmark diagnosis appears in the ranked Top-3. \emph{Criterion verification} evaluates each configured diagnosis against its disorder-specific criteria. Diagnoses that satisfy the study's operational compatibility rules are retained in a \emph{criterion-compatible set}. \emph{Primary selection} then asks which candidate is selected as the primary diagnosis; its observed output is the \emph{committed primary diagnosis}. These measures locate disagreements among output views and do not imply that every policy executes a fixed chronological funnel.

Throughout this thesis, \emph{primary selection} denotes the scientific task of choosing one primary diagnosis among plausible candidates. \emph{Finalization} denotes the system-level process that converts the recorded ranking, checker states, and compatibility outputs into the final output; Direct-Answer and Nominate-then-Select are finalization policies. \emph{Commitment} is reserved for the resulting output state, as in \emph{committed primary diagnosis} or \emph{committed Top-1 agreement}, rather than used as the name of a separate diagnostic stage.

We use the term \emph{primary-selection bottleneck} for the operational pattern in which a benchmark diagnosis is present among leading candidates or criterion-supported alternatives but the system selects a different committed primary diagnosis. The bottleneck is therefore a benchmark-local pattern showing that candidate coverage or criterion compatibility can exceed committed Top-1 agreement; it is not a claim about the true clinical primary diagnosis. It does not show that every error passes both earlier checks, that the corpus identifies a unique clinical primary diagnosis, or that every transcript contains enough evidence for one.\ct{what problem?}

This distinction matters because different errors need different solutions. Missing candidates call for better generation or retrieval. A candidate that fails criterion checking points to the checker, the rules, or missing evidence. A candidate that remains available but is not selected points to the final comparison among plausible diagnoses. The detailed metric definitions are given in \Cref{sec:gold-label-conventions}.

\section{Overview of HiED and Study Scope}

To make these failure modes observable, this thesis introduces HiED, a hybrid multi-agent system for Chinese psychiatric differential diagnosis from initial-consultation transcripts. The intended context is Taiwanese outpatient care, but all quantitative results in this thesis use synthetic Chinese transcript datasets; no Taiwanese patient results are reported.

HiED has five main parts. It formats the dialogue while preserving speaker turns, optionally retrieves similar examples, asks an LLM diagnostician for a ranked differential and a primary diagnosis, checks each configured diagnostic category against operationalized ICD-10 criteria, and applies deterministic rules to form a criterion-compatible set. Each criterion is marked as \texttt{met}, \texttt{not\_met}, or \texttt{insufficient\_\allowbreak{}evidence}.

The Direct-Answer policy keeps the diagnostician's primary diagnosis and uses outputs from the Criterion Checker and Compatibility Auditor as audit artifacts. The alternative Nominate-then-Select policy uses the same upstream outputs but allows the criterion-compatible set to determine a different committed diagnosis. Their comparison tests whether criterion compatibility is more useful as an audit or as a selector. Chapter~\ref{ch:architecture} describes the architecture in detail.

\section{Contributions}

This thesis makes two main contributions.

First, it presents an auditable hybrid architecture that combines LLM diagnosis, explicit ICD-10 criterion checking, and deterministic rules. The system records criterion states and supporting evidence instead of relying only on a free-form rationale. The trace can be inspected and replayed, but it does not prove that the LLM's hidden reasoning is faithful or that the checker judgments are clinically correct~\citep{turpin2023unfaithful}.

Second, it presents a stage-wise failure-localization and empirical evaluation framework that separates candidate detection, criterion verification, and primary selection, quantifies the primary-selection bottleneck, and tests whether criterion-based finalization and several primary-selection methods close the observed gap. The absence of improvement under the evaluated selection interventions narrows several simple explanations under the tested settings, but it does not show that better selectors or additional clinical information can never help.

\section{Thesis Organization}

Chapter~\ref{ch:related} reviews related psychiatric LLM, multi-agent, and auditable diagnosis research. Chapter~\ref{ch:architecture} describes HiED. Chapter~\ref{ch:data} defines the datasets and metrics, and Chapter~\ref{ch:experimental} explains the experiments. Chapters~\ref{ch:results} and~\ref{ch:external} report the internal and external synthetic results. Chapter~\ref{ch:error} examines error patterns. Chapters~\ref{ch:discussion}--\ref{ch:conclusion} discuss the findings, limitations, conclusions, and future work.


\chapter{Related Work}
\label{ch:related}

\section{Psychiatric LLM Diagnosis}

Psychiatric LLM studies cover several different tasks, including examination questions, short vignettes, diagnostic reasoning, and comparison with clinicians~\citep{qiu2023largeai,li2024taiwan,raballo2025,sarma2025}. These studies show that LLMs can produce plausible clinical answers, but performance depends on the input, label space, and reference standard. Detecting depression from text or audio is also different from choosing one primary diagnosis among several overlapping disorders~\citep{li2025addreview}.

Chinese psychiatric datasets make disorder-level evaluation possible. LingxiDiagBench includes static diagnosis and dynamic consultation over the synthetic LingxiDiag-16K corpus and reports a clear drop from broad depression--anxiety classification to fine-grained diagnosis~\citep{xu2026lingxidiagbench}. MDD-5k provides a second synthetic Chinese diagnostic-dialogue resource based on anonymized cases~\citep{yin2024mdd5k}. These datasets support reproducible experiments, but they do not replace real-patient validation.

Existing systems also differ in whether they can ask questions. MIND, WiseMind/ProAI, MAGI, and DSM5AgentFlow study inquiry or interactive diagnostic workflows~\citep{li2026mind,wu2026wisemind,bi2025magi,ozgun2025dsm5agentflow}. MentalSeek-Dx and MoodAngels focus more on diagnosis from information already available in records or transcripts~\citep{sun2026mentalseek,xiao2025moodangels}. Interactive systems can obtain missing evidence, while fixed-input systems isolate how well a model uses the same evidence. This thesis studies the fixed-input setting.

\section{Retrieval-Augmented Diagnostic Reasoning}

Retrieval-augmented generation conditions a model on external records selected for the current query rather than relying only on information encoded in model parameters~\citep{lewis2020rag}. In-context-learning studies similarly show that semantically related demonstrations can be retrieved to construct a query-specific prompt and that performance depends on which examples are selected~\citep{liu2022incontext}. In transcript-based diagnosis, retrieved cases can therefore provide prompt-side examples of symptom descriptions and diagnostic labels. They are demonstrations rather than patient evidence and should not directly determine the committed diagnosis.

Similarity retrieval can expose useful corpus-local regularities, but it can also reproduce the frequency and wording patterns of the indexed data. Ordinary nearest-neighbor retrieval may repeatedly select examples from common labels, while a parent-balanced policy changes the label composition of the prompt. It may also change the number, ordering, and total token length of the retrieved examples. A comparison between global and parent-balanced retrieval consequently evaluates complete retrieval configurations rather than isolating balancing as a single causal factor.

These considerations motivate the retrieval experiments in this thesis. No retrieval, global Top-5 retrieval, and parent-balanced retrieval are compared using the same training-set retrieval index, evaluation cases, label projection, and scoring implementation. The comparison is repeated in both the Single and HiED pipelines so that retrieval behavior is not inferred from only one architecture.

\section{Medical Multi-Agent Reasoning}

Medical multi-agent systems assign different roles to LLMs and combine their outputs through consultation, debate, or adaptive collaboration~\citep{tang2024medagents,kim2024mdagents}. AMIE and related work also show the value of broad differential diagnosis, realistic consultation, and expert review~\citep{mcduff2025amie,tu2025conversational}. These systems demonstrate that structured interaction can help, but adding agents does not guarantee a better final answer. Agents may repeat the same model bias, and free-form discussion can make it difficult to identify which component changed the decision.

For this thesis, the important issue is controlled attribution. If ranking, criterion checking, debate, and finalization all change at once, an accuracy difference cannot show which change helped. HiED therefore compares two policies that reuse the same ranked candidates and checker outputs: Direct-Answer keeps the diagnostician's decision, whereas Nominate-then-Select uses criterion compatibility to choose the primary.

\section{Auditability and Criterion Grounding}
\label{sec:auditability-definition}

A fluent explanation is not proof of faithful reasoning. Model-generated chains can sound coherent while not reflecting the factors that produced the answer~\citep{turpin2023unfaithful}. In healthcare, explanation quality, interpretability, and clinical validity therefore need separate evaluation~\citep{rudin2019stop,huang2024xiai}.

Diagnostic-chain and symptom-based methods make outputs easier to inspect~\citep{chen2025cod,lee2026interpretable,lyu2026depllm}. However, a generated reasoning chain is still another model output. This thesis uses the narrower term \emph{auditability}: an external record that links transcript evidence, criterion states, rule results, and the final output. The record can be checked and replayed, but it does not reveal the model's hidden reasoning or turn checker outputs into clinical ground truth.

Constraint-logic systems are the closest related approach because they translate diagnostic criteria into explicit rules~\citep{kim2025clp}. HiED keeps the learned diagnostician separate from the criterion and rule layer. Under Direct-Answer, the structured layer audits the decision; under Nominate-then-Select, it is tested as a selector. Among the reviewed studies, we did not identify an evaluation that uses this separation to compare retention of a benchmark diagnosis with primary selection on the same Chinese psychiatric transcripts.

\section{Structured Diagnostic Knowledge and Primary Selection}

Knowledge-based diagnosis methods represent relations among symptoms, disorders, and missing information. MedKGI supports information-guided inquiry, and DKEC uses structured domain knowledge for label prediction~\citep{wang2025medkgi,ge2024dkec}. Such relations may help when disorders share symptoms but differ in onset, duration, exclusions, or causal direction.

HiED does not implement a full knowledge-graph selector. Instead, this literature motivates future work if simple criterion counts and local re-ranking are not enough. A stronger selector may need to compare which diagnosis best explains the full presentation, not only how many criteria each diagnosis satisfies.

\clearpage
Table~\ref{tab:related-work-matrix} does not compare overall system quality. It compares whether each study exposes the intermediate outputs needed to distinguish candidate-generation, criterion-verification, and primary-selection errors.

\begin{table}[htbp]
\centering
\caption{Comparison of representative psychiatric diagnostic systems by the outputs available for stage-wise failure localization.}
\label{tab:related-work-matrix}
\scriptsize
\begingroup
\setlength{\tabcolsep}{2pt}
\begin{adjustbox}{max width=\textwidth}
\begin{tabular}{|p{2.2cm}|p{1.9cm}|p{1.35cm}|p{2.35cm}|p{3.2cm}|p{3.7cm}|}
\hline
Study or system &
Evidence setting &
Follow-up questioning &
Ranked differential &
Criterion or evidence audit &
Separate evaluation of candidate detection, criterion verification, and primary selection\\
\hline
MIND~\citep{li2026mind} &
Interactive psychiatric consultation &
Yes &
Not reported as a ranked output &
Criteria-grounded rationales and process rewards &
No---final diagnosis and dialogue/process metrics\\
\hline
WiseMind/ProAI\newline\citep{wu2026wisemind} &
Interactive simulated and real-user dialogue &
Yes &
Not reported as a ranked output &
DSM-guided action states for met criteria, unmet criteria, missing details, and contradictions &
Partial---critical-node recall and final diagnostic accuracy, without candidate-detection-to-primary-selection decomposition\\
\hline
MAGI~\citep{bi2025magi} &
Interactive MINI-guided interview &
Yes &
Not reported as a ranked output &
PsyCoT maps symptoms and dialogue evidence to diagnostic criteria &
Partial---interview, PsyCoT, and final diagnosis metrics, without a ranked-candidate stage\\
\hline
DSM5AgentFlow\newline\citep{ozgun2025dsm5agentflow} &
Simulated interactive questionnaire &
Yes &
Not reported as a ranked output &
Rationale links utterances to supporting or contradictory DSM-5 criteria &
Partial---diagnostic and explanation metrics, without candidate-stage evaluation\\
\hline
MentalSeek-Dx\newline\citep{sun2026mentalseek} &
Fixed de-identified clinical record &
No &
Hypothesis list generated; ranking not separately reported as a test metric &
Criteria-guided staged reasoning, without case-level criterion states linked to evidence &
Partial---category, disorder, and joint accuracy, without criterion-retention analysis\\
\hline
MoodAngels~\citep{xiao2025moodangels} &
Fixed records and assessment scales &
No &
Not reported as a ranked output &
DSM-5 criterion retrieval and granular symptom matching &
Partial---agent and component results, without candidate--verification--selection evaluation\\
\hline
Constraint-logic CDSS~\citep{kim2025clp} &
Structured patient facts &
No &
No &
Inspectable, expert-corrected Datalog diagnostic rules &
No---program fidelity and final diagnosis accuracy\\
\hline
Ours &
Fixed transcript &
No &
Ranked Top-5; Top-3 evaluated &
Criterion states linked to transcript evidence &
Yes---candidate detection, retention of the benchmark diagnosis after verification, and primary selection evaluated on the same cases\\
\hline
\end{tabular}
\end{adjustbox}
\endgroup
\end{table}

\noindent\textit{Note:} ``Not reported'' indicates that the output or evaluation was not described in the reviewed publication; it does not establish that the underlying implementation lacks the capability.

The comparison shows that prior systems address complementary parts of the diagnostic process. Interactive systems can collect missing information, whereas fixed-input systems permit controlled evaluation under identical evidence. Some methods expose candidate hypotheses or criterion-related outputs, but the reviewed studies do not jointly evaluate candidate detection, criterion verification with retention of the benchmark diagnosis, and primary selection on the same fixed cases. HiED is designed to make these three output views separately measurable rather than to claim superiority across all clinical or conversational capabilities.

\section{Research Gap and Research Questions}

Prior work shows that LLMs can diagnose psychiatric cases, retrieve examples, use diagnostic criteria, and work in multi-agent systems. Five gaps remain. First, conventional text classifiers, single LLMs, retrieval-augmented LLMs, and structured systems are not always compared under the same split and endpoint. Second, final accuracy does not show whether a benchmark diagnosis was absent from the candidate list or present but not selected as the committed primary. Third, global nearest-neighbor retrieval and parent-balanced retrieval require matched comparison across architectures. Fourth, criterion-based finalization and later primary-selection methods must be evaluated separately from candidate generation and retrieval. Fifth, it remains unclear whether the observed Top-3--Top-1 selection gap persists across model sizes, external corpora, and changes in the configured diagnostic scope. The precise gap addressed here is that HiED retains candidate detection, criterion verification, and primary selection as separately observable stages evaluated on the same cases.

This thesis therefore uses five research questions:

\begin{description}[style=nextline,leftmargin=3cm,labelwidth=2.5cm]
\item[RQ1] How does HiED compare with conventional and single-model baselines under the same data split, label mapping, and primary evaluation endpoint?
\item[RQ2] How often is a benchmark diagnosis present among ranked candidates or criterion-supported alternatives but not selected as the committed diagnosis?
\item[RQ3] How do no retrieval, global Top-5 retrieval, and parent-balanced retrieval compare in matched Single and HiED pipelines?
\item[RQ4] Do criterion-based re-selection or the tested primary-selection methods improve over Direct-Answer?
\item[RQ5] To what extent do the main findings persist across model sizes, external synthetic corpora, and changes in the configured diagnostic scope, and what error patterns characterize the remaining failures?
\end{description}
\ct{Does your thesis really answer all 5 questions?}

RQ1 and RQ3 use matched comparisons on the same fixed internal test set, with common label projection and evaluation code. Clinical effectiveness, transcript-only human performance, and criterion-level clinical validity remain future work.

\chapter{HiED System Architecture}
\label{ch:architecture}

\section{Input and Diagnostic Scope}

HiED processes one Chinese psychiatric consultation transcript at a time. The input is an ordered sequence of turns, each marked as \texttt{Clinician} or \texttt{Patient}; speaker labels and turn order are preserved in the prompt. Transcripts longer than the available prompt budget are deterministically truncated by retaining complete turns from the beginning and end, with an explicit marker for the omitted middle. The system does not summarize or invent missing text. This is a fixed-input study: the system cannot ask the patient a follow-up question during inference.\ct{Is this about input and transcript format?}

ICD-10 is the diagnostic coding system used to name the disorders in this study~\citep{who2019icd10}. The system does not search all ICD-10 diagnoses. Before a run begins, it loads one fixed \emph{configured diagnostic profile} that is used for every patient. The main diagnostic profile contains fourteen configured diagnostic categories. F41 and F43 include subtype-level categories, so the fourteen categories are projected to eleven broader parent labels for scoring. Chapter~\ref{ch:data} defines how mixed in-scope and out-of-scope dataset-provided diagnosis codes are projected for evaluation.

Three terms are used throughout the architecture. A \emph{configured diagnostic category} is an ICD-10 code that the diagnostician may rank and for which the system defines a corresponding criterion checker. The \emph{criterion-checker set} is the collection of those fourteen checker definitions; it is not a learned memory bank. Each criterion receives one of three model-generated evidence states: \texttt{met} when the transcript supports it, \texttt{not\_met} when the transcript contains evidence against it, and \texttt{insufficient\_\allowbreak{}evidence} when the transcript supports neither conclusion. The rules are study-specific operationalizations informed by ICD-10 descriptions, not a reproduction of the full official manual and not independently clinician-validated ground truth.

\begin{table}[htbp]
\centering
\caption{Configured ICD-10 diagnostic categories and their scoring parent labels.}
\label{tab:configured-profile-summary}
\small
\begin{tabularx}{\textwidth}{|p{3.0cm}|p{2.2cm}|X|}
\hline
ICD-10 code & Scoring parent & Diagnostic category\\
\hline
F20 & F20 & Schizophrenia\\
\hline
F31 & F31 & Bipolar affective disorder\\
\hline
F32 & F32 & Depressive episode\\
\hline
F39 & F39 & Unspecified mood disorder\\
\hline
F41.0 & F41 & Panic disorder\\
\hline
F41.1 & F41 & Generalized anxiety disorder\\
\hline
F41.2 & F41 & Mixed anxiety and depressive disorder\\
\hline
F42 & F42 & Obsessive-compulsive disorder\\
\hline
F43.1 & F43 & Post-traumatic stress disorder\\
\hline
F43.2 & F43 & Adjustment disorders\\
\hline
F45 & F45 & Somatoform disorders\\
\hline
F51 & F51 & Nonorganic sleep disorders\\
\hline
F98 & F98 & Other behavioral and emotional disorders\\
\hline
Z71 & Z71 & Counseling without a specific disorder\\
\hline
\end{tabularx}
\end{table}

\section{System Overview and Output Artifacts}

The diagnostician prompt contains four readable parts: the task and the fixed list of configured diagnostic categories; the speaker-labeled transcript; retrieved training examples, when retrieval is enabled; and the required structured output fields. Retrieval is completed before generation. The model sees only the current transcript and the supplied examples.

\ThesisFigure[1.0\textwidth]{fig_pipeline_round20c.pdf}{Overview of the HiED pipeline and its intermediate artifacts. Direct-Answer keeps the Diagnostician's emitted primary diagnosis, whereas Nominate-then-Select uses Criterion Checker and deterministic rule-layer artifacts to determine the committed diagnosis.}{fig:pipeline-thesis-rewrite}

\begin{table}[htbp]
\centering
\caption{Functions and interfaces of the blocks shown in Figure~\ref{fig:pipeline-thesis-rewrite}.}
\label{tab:fig31-block-functions}
\small
\begin{tabularx}{\textwidth}{|p{2.8cm}|p{3.0cm}|p{4.1cm}|X|}
\hline
Block & Main input & Function & Output and downstream use\\
\hline
Transcript Input & Speaker-labeled transcript & Preserves turn order, applies the fixed prompt-budget rule, and attaches the configured diagnostic profile & Supplies the Diagnostician and Criterion Checker prompts\\
\hline
Similar-Case Retriever & Transcript and training-set retrieval index & Finds prompt-side examples; retrieved cases go only to the Diagnostician & Optional examples, never direct finalization evidence\\
\hline
Diagnostician & Transcript, profile, and optional retrieved examples & Produces a ranked differential diagnosis and emitted primary & Ranked candidate list, primary field, optional comorbid field\\
\hline
Criterion Checker & Transcript and category-specific criteria & Assigns \texttt{met}, \texttt{not\_met}, or \texttt{insufficient\_\allowbreak{}evidence} states & Criterion trace for audit and deterministic compatibility rules\\
\hline
Compatibility Auditor & Checker states and fixed rules & Deterministic rule application over each configured diagnostic category & Criterion-compatible diagnoses and rule trace\\
\hline
Committed Diagnosis & Ranked candidate list, emitted primary, compatible set, and policy & Applies DA or NtS finalization & Final primary diagnosis depends on the selected finalization policy\\
\hline
Audit Artifacts & Candidates, checker states, rules, and finalization source & Records inspectable intermediate outputs & Offline review only; Audit Artifacts do not alter the Direct-Answer committed primary output\\
\hline
\end{tabularx}
\end{table}

In this thesis, an \emph{agent} means an LLM call with a distinct prompt and task. The checker agents run independently; they do not have autonomous memory, planning, or free-form negotiation. HiED is therefore a modular multi-stage LLM orchestration with role-specific agents and deterministic rule aggregation.

\section{Candidate Generation and Retrieval}
\label{sec:retrieval}

The primary diagnostician configuration uses Qwen3-32B with 4-bit Activation-aware Weight Quantization (AWQ). Its input contains the transcript, the configured diagnostic profile, and optional retrieved examples. Its prompt requests a ranked candidate list containing five ordered diagnoses from that profile, a short reason for each, one primary field chosen from the first three, and at most one separate comorbid field. In clinical prose this is a ranked differential diagnosis; as a formal artifact it is the ranked candidate list. The primary field and the ranked list are stored separately. In the running example, F41.1 can therefore remain in the Top-3 even though F32 is emitted as primary.

The main diagnostician configuration is a system specification, not an ablation. It uses greedy decoding and does not request a calibrated probability or an abstention decision. Confidence-based sampling, calibration, and selective-output approaches are evaluated only as additional selection analyses or future-work directions.\ct{Is this an ablation study? If so, why in Appendix?}

The Similar-Case Retriever adds relevant training examples to the diagnostician prompt. It follows the retrieval-augmented generation pattern~\citep{lewis2020rag}: BGE-M3 is the multilingual text-embedding model used to convert the query transcript and training transcripts into normalized dense vectors~\citep{chen2024bgem3}. FAISS is the vector similarity-search library and index used for retrieval, not a standalone data store~\citep{douze2024faiss}. FAISS inner-product search over normalized vectors corresponds to cosine-similarity ranking. The study evaluates no retrieval, global Top-5 retrieval, and parent-balanced retrieval; the final retrieval setting is selected using the public validation split.

The retrieval classes are fixed by the configured diagnostic profile and are identical for every patient.\ct{Are the candidate classes pre-determined, or patient-specific?} Only the example selected within each class depends on the current transcript. Subtypes that share a parent code, such as F41.0, F41.1, and F41.2, share one retrieval class. Global Top-5 always returns the five nearest eligible index entries. Parent-balanced retrieval attempts to return at most one high-similarity example for each of the eleven scoring parents and can therefore supply up to eleven examples. This policy is intended to reduce domination by frequent classes, but it also changes example count, label composition, ordering, prompt length, and possibly the available transcript budget. Retrieved cases are included only in the Diagnostician prompt; they do not enter checker prompts or directly choose the final output.

The three conditions use the same training-set retrieval index and embedding model. Their comparison therefore evaluates complete retrieval configurations rather than isolating parent balancing as a single causal factor.

\section{Criterion Verification and Compatibility Aggregation}

Each of the fourteen configured diagnostic categories has a corresponding criterion checker based on the study's operational rule set.\ct{Is this ICD 10?} The rules are linked to ICD-10 disorder descriptions~\citep{who2019icd10}, but several thresholds are simplified study rules. Appendix~\ref{app:configured-profile} lists the diagnostic codes and configured thresholds so readers can distinguish the study implementation from official clinical guidance.

For each criterion, the checker returns \texttt{met} when the transcript supports the criterion, \texttt{not\_met} when the transcript contains evidence against it, and \texttt{insufficient\_\allowbreak{}evidence} when the available transcript does not resolve it.\ct{What 3 states?} It may also return a short transcript span linked to the judgment. Each configured diagnostic category is checked as an independent task using the transcript and its category-specific operational criteria. Retrieved examples do not enter checker prompts, and checkers do not communicate with one another. All fourteen configured categories are checked for every case, yielding one category-level audit record for each.

Missing information is not negative evidence. A criterion is \texttt{not\_met} only when the transcript contains evidence against it; otherwise unresolved evidence remains \texttt{insufficient\_\allowbreak{}evidence}.

The Compatibility Auditor is a deterministic rule-based module. For each configured diagnostic category, it reads the Criterion Checker states, applies the predefined compatibility rule, adds the diagnosis to the criterion-compatible set when that rule is satisfied, and retains the rule application in the audit trace. A rule may require all criteria, a minimum number of core and total criteria, a symptom count, or particular criterion groups. The same checker trace always produces the same compatible set. Compatibility with a configured study rule is not primacy and does not mean that the diagnosis is clinically correct or should be primary. Several diagnoses may be designated compatible in the same case.

Checker states are model-generated and have not been validated as clinician ground truth. They create an evidence-linked record that can be inspected and replayed, but they do not prove that the criterion judgments or evidence spans are correct.

\section{Primary Selection and Finalization}

HiED defines two finalization policies over the same upstream ranked candidates, criterion states, and criterion-compatible set. Direct-Answer (DA) is the default policy and preserves the diagnostician's emitted decision. Nominate-then-Select (NtS) tests whether criterion-derived compatibility can improve the committed primary diagnosis and emits one selected diagnosis.

Under Direct-Answer (DA), the diagnostician's primary field becomes the committed diagnosis; the first ranked diagnosis is used only when that field is missing. An optional comorbidity is retained when it differs from the primary. Outputs from the Criterion Checker and Compatibility Auditor do not replace the DA committed diagnosis.

Nominate-then-Select (NtS) uses criterion compatibility to choose the committed diagnosis. For configured category $d$, its criterion-support ratio is
\[
\operatorname{met\_ratio}(d)=\min\!\left(1,\frac{\text{number of \texttt{met} criteria for }d}{\text{required criterion count for }d}\right).
\]
The denominator is the required count stored in the configured rule, not the total number of listed criteria. NtS first selects the earliest criterion-compatible diagnosis in the five-diagnosis ranking. If none of those five is criterion-compatible, it selects another criterion-compatible diagnosis by descending met ratio, with ties resolved by the predefined configured-category order. If no diagnosis is criterion-compatible, it falls back to the first ranked diagnosis.

NtS emits only the re-selected primary. DA and NtS therefore reuse the same ranked candidates and checker traces but differ in both the primary-selection rule and secondary-label emission. The primary analysis consequently uses Top-1 Accuracy. Exact Match and classwise F1 for NtS also reflect its single-label output contract and are not interpreted as isolated effects of primary re-selection. Additional primary-selection methods are defined in Chapter~\ref{ch:experimental} and do not alter the core architecture described here.

The audit artifacts include the predicted diagnosis set, ranked differential, criterion states, short evidence spans, criterion-compatible diagnoses, finalization source, and execution failures. A reviewer can therefore challenge the candidate ranking, an individual criterion judgment, a study rule, or the final selection separately.

This record follows the definition of auditability in Section~\ref{sec:auditability-definition}. It makes the system easier to inspect, but it does not expose the LLM's hidden reasoning, prove clinical correctness, or replace real-patient validation.

\section{Worked Example}
\label{sec:running-example}

A short worked example shows how the same upstream artifacts can produce different committed primary diagnoses under the two finalization policies. The example is schema-faithful and rule-faithful, but it is neither a patient record nor an exported model trace.

\begin{quote}
\small
The patient reports approximately two weeks of depressed mood, loss of interest, early awakening, fatigue, and poor concentration. Persistent worry is also described, but its duration and onset relative to the low mood remain unclear. Motor tension, autonomic symptoms, and the relationship between the mood and anxiety syndromes are not established. Manic symptoms and suicidal thoughts are denied.
\end{quote}

\ThesisFigure[1.0\textwidth]{fig_worked_example_flow.pdf}{Worked example of ranking, criterion verification, and primary selection. Direct-Answer preserves the Diagnostician's emitted primary diagnosis, whereas Nominate-then-Select chooses the highest-ranked diagnosis in the criterion-compatible set. Both policies use the same upstream evidence. The example illustrates the system contract and does not constitute clinical review of the preferred diagnosis.}{fig:worked-example-flow}

\begin{table}[htbp]
\centering
\caption{Criterion-verification summary for the worked example.}
\label{tab:running-example}
\small
\begin{tabularx}{\textwidth}{>{\raggedright\arraybackslash}p{3.2cm}>{\raggedright\arraybackslash}X>{\raggedright\arraybackslash}X}
\toprule
\textbf{Output} & \textbf{F41.1 Generalized anxiety disorder} & \textbf{F32 Depressive episode}\\
\midrule
Diagnostician rank & 1 & 2\\
Main supporting evidence & Worry, concentration difficulty, and sleep disturbance & Depressed mood, anhedonia, two-week duration, fatigue, concentration difficulty, and sleep disturbance\\
Main unresolved evidence & Six-month duration and broader anxiety features & Several additional associated symptoms are not discussed\\
Criterion-verification result & \textbf{Not retained} & \textbf{Criterion-compatible}\\
\bottomrule
\end{tabularx}
\end{table}

Figure~\ref{fig:worked-example-flow} makes the policy divergence explicit. The Diagnostician ranks F41.1 first and F32 second. Criterion verification retains F32 but not F41.1 because key anxiety-duration and associated-feature evidence remains unresolved. DA preserves the emitted F41.1 primary, whereas NtS selects F32 as the highest-ranked criterion-compatible diagnosis. These outputs illustrate the policy contracts, not a claim that F32 is the clinically correct primary diagnosis.

\section{Implementation Configuration}

The main system uses Qwen3-32B with 4-bit Activation-aware Weight Quantization (AWQ). AWQ reduces model-memory requirements and allows the 32-billion-parameter backbone to be served locally without changing the prompt, diagnostic scope, or finalization logic. No main-system component fine-tunes model weights. vLLM is the inference and serving backend for loading the model and generating outputs; it is not a diagnostic component and does not alter prompts, criteria, rules, or finalization. The system uses greedy decoding, an 8,192-token context window, a 1,536-token output cap, structured JSON output, guided decoding where supported, schema validation, and checker-output recovery for duplicate or invalid criterion items.

Runs are checked for case count, valid outputs, parse failures, ranked-output availability, and candidate validity. A checker-output recovery occurs when an LLM response can be parsed but contains duplicate or invalid criterion items. The parser removes those items and retains the valid criterion records. This is a structured-output schema-adherence warning rather than a hard parse failure.

\chapter{Datasets, Label Projection, and Evaluation Protocol}
\label{ch:data}

\section{Configured Output Space and Gold-Label Projection}
\label{sec:gold-label-conventions}

Throughout this thesis, the dataset-provided diagnoses used for scoring are referred to as \emph{benchmark gold labels}. The term \emph{gold} denotes the official benchmark reference and does not imply a uniquely correct clinical diagnosis.

The main system uses fourteen configured ICD-10 diagnostic categories, listed in Table~\ref{tab:configured-profile-summary}. For evaluation, subtype-level categories are projected to eleven broader parent labels. Let $G_{\mathrm{raw}}(x)$ be the raw benchmark gold codes for case $x$, let $\pi$ map subtype codes such as F41.0, F41.1, and F41.2 to their scoring parent, and let $\mathcal{C}$ be the set of eleven configured scoring parents. The in-scope gold-label set is
\[
G_{\mathrm{in}}(x)=\{\pi(c):c\in G_{\mathrm{raw}}(x),\ \pi(c)\in\mathcal{C}\}.
\]
The scored gold-label set is then
\[
G(x)=
\begin{cases}
G_{\mathrm{in}}(x), & G_{\mathrm{in}}(x)\neq\varnothing,\\
\{\mathrm{Others}\}, & G_{\mathrm{in}}(x)=\varnothing.
\end{cases}
\]
Thus, a mixed case such as F90 with F41.1 is scored as $\{\mathrm{F41}\}$ rather than $\{\mathrm{Others},\mathrm{F41}\}$. A case is in scope when $G_{\mathrm{in}}(x)$ is nonempty and out of scope otherwise. Because the main system has neither an \emph{Others} output nor a trained unknown detector, it cannot correctly represent an out-of-scope case.

For case $x$, $G(x)$ is the deduplicated gold-label set after parent-level projection. LingxiDiag cases may contain more than one gold parent label, and neither dataset provides a psychiatrist-reviewed unique clinical primary diagnosis. The first-listed gold parent is used only for explicitly single-label descriptive analyses such as the confusion matrix.

In the running example from Chapter~\ref{ch:architecture}, F41.1 is scored as F41. If the gold-label set contains F41 and the system emits F32 while ranking F41.1 second, Top-1 is wrong but Top-3 is correct.

\section{Datasets, Case Construction, and Split Roles}

The study uses two synthetic Chinese psychiatric dialogue datasets. LingxiDiagBench provides the in-domain training, development, and internal test data. MDD-5k provides an external synthetic test set.

\subsection{LingxiDiagBench}

LingxiDiagBench contains synthetic Chinese psychiatric consultations aligned with electronic medical records~\citep{xu2026lingxidiagbench}. The benchmark includes binary, four-class, and twelve-class tasks and is used here because it provides a fixed label space and enough cases for training, development, and internal evaluation. The published benchmark shows that fine-grained diagnosis is harder than broad depression--anxiety classification.

The accessible data contained 14,000 training cases and 1,000 public validation cases. The official test file was unavailable when the experiments were designed. Before the main internal evaluation, 1,000 cases were set aside from the training source. The carve was stratified by first-listed projected parent to reproduce the public-validation distribution, using largest-remainder allocation and sampling without replacement with NumPy seed 42. The ordered case-ID fingerprint is \texttt{317a7633a339ae9a}. Exact case-ID uniqueness and overlap checks were applied, including exclusion from the final retrieval index; no separate semantic near-duplicate screen was applied. The carved cases were not used for prompt, rule, or final retrieval-configuration selection.

\begin{table}[htbp]
\centering
\caption{LingxiDiag data splits and their roles in this thesis.}
\label{tab:data-roles-rewrite}
\begin{tabularx}{\textwidth}{|>{\raggedright\arraybackslash}p{4.0cm}|r|>{\raggedright\arraybackslash}X|}
\hline
Split & $N$ & Use\\
\hline
Training-source retrieval corpus & 13,000 & Builds the training-set retrieval index and trains conventional baselines; not used as a scored test set\\
\hline
Public validation split & 1,000 & Selects prompts, rules, sensitivity settings, and the final retrieval configuration; also provides descriptive same-split context\\
\hline
Internal held-out split & 1,000 & Reports the main in-domain evaluation after configuration selection\\
\hline
\end{tabularx}
\end{table}

All internal methods are evaluated on the same fixed set of 1,000 test cases. Because the public validation split contributes to configuration selection, its results are not treated as an independent test estimate.

\subsection{MDD-5k}

MDD-5k was generated from anonymized psychiatric cases through a neuro-symbolic multi-agent process~\citep{yin2024mdd5k}. Each patient file may contain several dialogue rounds. The adapter joins those rounds and treats the patient file as one case.

MDD-5k contains 925 matched patient-level cases. Under the configured parent-label set defined in Section~\ref{sec:gold-label-conventions}, 878 cases are in scope and 47 are out of scope. Additional checker definitions are used only in the later scope-expansion experiment and are not enabled in the primary analysis configuration.

The 490-case F32-versus-F41 subset tests coarse depression--anxiety discrimination; this subset excludes F41.2 and records containing both F32 and F41. MDD transcripts are substantially longer, and the deterministic prompt-budget truncation rule is applied in the primary external evaluation. Both datasets are synthetic and are used for controlled benchmark evaluation rather than clinical validation.

\subsection{Evaluation-Set Label Distributions}

\begin{table}[htbp]
\centering
\caption{Distribution of first-listed projected gold labels across the internal and external synthetic evaluation sets.}
\label{tab:class-distribution}
\small
\begin{adjustbox}{max width=\textwidth}
\begin{tabular}{|l|l|r|r|r|}
\hline
Scoring label & Label description &
\shortstack{Internal test\\($N=1000$)} &
\shortstack{MDD-5k in-scope\\($N=878$)} &
\shortstack{MDD-5k out-of-scope\\($N=47$)}\\
\hline
F20 & Schizophrenia & 5 & 3 & 0\\
\hline
F31 & Bipolar affective disorder & 9 & 11 & 0\\
\hline
F32 & Depressive episode & 365 & 410 & 0\\
\hline
F39 & Unspecified mood disorder & 60 & 64 & 0\\
\hline
F41 & Anxiety disorders & 358 & 313 & 0\\
\hline
F42 & Obsessive-compulsive disorder & 25 & 13 & 0\\
\hline
F43 & Stress-related disorders & 11 & 7 & 0\\
\hline
F45 & Somatoform disorders & 8 & 7 & 0\\
\hline
F51 & Nonorganic sleep disorders & 36 & 18 & 0\\
\hline
F98 & Other behavioral and emotional disorders & 30 & 24 & 0\\
\hline
Z71 & Counseling without a specific disorder & 8 & 8 & 0\\
\hline
Others & Out-of-scope gold diagnosis & 85 & 0 & 47\\
\hline
 & Total & 1000 & 878 & 47\\
\hline
\end{tabular}
\end{adjustbox}
\end{table}

\noindent\textit{Note:} Cases may contain more than one projected gold label. For this descriptive distribution, each case is assigned to its first-listed projected gold label so that each column sums to the corresponding number of cases. The table therefore does not report multilabel prevalence. \emph{Others} is an evaluation category assigned when none of a case's dataset gold diagnoses is represented in the configured output space; it is not a diagnosis that HiED can emit.

\section{Prediction Views and Metrics}

The evaluator keeps three outputs separate:

\begin{enumerate}[label=(\arabic*)]
\item The \emph{primary view}, $p_1(x)$, is the final primary diagnosis.
\item The \emph{ranked view}, $R_3(x)$, contains the first three distinct parent labels in the method's genuine ranked output. For HiED, it comes from the recorded Diagnostician ranking and is independent of any later DA or NtS finalization decision.
\item The \emph{multilabel view}, $M(x)$, contains the primary and any emitted comorbid parent.
\end{enumerate}

TF--IDF + logistic regression obtains its Top-3 from ranked class scores. HiED obtains it from the Diagnostician ranking. A Single system without a genuine ranked output is not assigned an artificial Top-3.

Because a case may contain more than one gold label, Top-$k$ Accuracy counts a case as correct when at least one gold label appears among the first $k$ predictions. Exact Match, also called subset accuracy, requires the complete predicted label set to equal the complete gold-label set. These are benchmark-agreement measures, not independent measures of the patient's true clinical diagnosis.

\begin{table}[htbp]
\centering
\caption{Metric contract for the main output views.}
\label{tab:metric-contract}
\small
\begin{tabularx}{\textwidth}{|p{2.4cm}|p{3.0cm}|p{4.0cm}|X|}
\hline
Measure & Output view & Correct when & Question answered\\
\hline
Top-1 Accuracy & Committed primary & Primary is in the gold-label set & Did the committed primary match any gold parent label?\\
\hline
Top-3 Accuracy & Genuine ranked Top-3 & Any top-three candidate is in the gold-label set & Did the genuine ranked Top-3 contain any gold parent label?\\
\hline
Gold-label retention & Criterion-compatible set & Any gold parent is criterion-compatible & Was any gold parent retained as compatible under the configured rules?\\
\hline
Exact Match & Multilabel output & Predicted set equals gold-label set & Did the complete set match?\\
\hline
Macro-F1 & Multilabel output & Mean one-vs-rest label F1, equal class weight & How does label-level performance average across classes?\\
\hline
Weighted-F1 & Multilabel output & Mean one-vs-rest label F1, weighted by gold-label support & How does label-level performance reflect the observed class distribution?\\
\hline
\end{tabularx}
\end{table}

Top-3 and committed Top-1 are computed over all 1,000 internal test cases. Gold-label retention in the criterion-compatible set is computed over the 915 cases with at least one non-\emph{Others} gold parent covered by a corresponding Criterion Checker.

\[
\mathrm{Top\text{-}1\ Accuracy}=\frac{1}{N}\sum_x \mathbb{1}[p_1(x)\in G(x)],
\]
\[
\mathrm{Top\text{-}3\ Accuracy}=\frac{1}{N}\sum_x \mathbb{1}[R_3(x)\cap G(x)\neq\varnothing],
\]
\[
\mathrm{Exact}=\frac{1}{N}\sum_x \mathbb{1}[M(x)=G(x)].
\]

Macro-F1 is the unweighted mean of the twelve one-vs-rest label-level F1 scores obtained by binarizing $G(x)$ and $M(x)$ over the fixed scoring classes. Weighted-F1 uses the same label-level F1 scores but weights each class by its gold-label support.

First-listed-label agreement compares the committed diagnosis only with the first-listed projected gold parent. It is used for the confusion matrix but not for the main result. It may differ from any-gold Top-1 Accuracy when a case contains more than one gold parent label.

\section{Failure-Localization Indicators}

For each case, the thesis records three indicators:
\[
D_3(x)=\mathbb{1}[R_3(x)\cap G(x)\neq\varnothing],
\]
\[
R(x)=\mathbb{1}[L(x)\cap G(x)\neq\varnothing],
\]
\[
S(x)=\mathbb{1}[p_1(x)\in G(x)],
\]
where $L(x)$ is the criterion-compatible parent set. $D_3$ and $S$ use all cases. Gold-label retention, $R$, is calculated only for cases whose gold-label set contains at least one non-\emph{Others} parent represented by a corresponding Criterion Checker. This checker-eligible subset contains 915 of the 1,000 test cases, whereas Top-1 and Top-3 use all 1,000 cases. The rates therefore have different denominators and should not be interpreted as consecutive stages of one case-level funnel.

These indicators ask three simple questions: Was a gold label in the genuine ranked Top-3? Was it retained as compatible under the configured rules? Was it selected as primary? They are analysis views of one recorded output, not a claim that every finalization policy executes the steps in that order.

\section{Statistical Analysis}

The case is the unit of analysis, and the two-sided significance level is $\alpha=0.05$. Single-system binary proportions are summarized with 95\% Wilson confidence intervals. Pairwise comparisons require identical case IDs and endpoint definitions. For binary correctness outcomes, the point estimate is the paired difference in correctness proportions, and the exact McNemar test is computed from the two discordant counts: cases correct only under the first system and cases correct only under the second. Main paired comparisons report the effect and confidence interval; detailed discordant counts and $p$-values are retained in the supporting appendix.

For paired accuracy differences and finalization interaction contrasts, case IDs are resampled with replacement using 10,000 paired bootstrap replicates and seed 42. The 2.5th and 97.5th percentiles form the reported 95\% confidence interval. Reported $p$-values are interpreted together with discordant counts, confidence intervals, and effect direction rather than as standalone evidence.

Holm correction is applied separately within the comparison families defined in Chapter~\ref{ch:experimental}; unrelated research questions are not pooled into one family. A non-significant result indicates insufficient evidence of a difference and is not treated as proof of equivalence. Paired tests require identical cases and endpoint definitions. When systems also differ in context budget, number of model calls, or other execution settings, the paired result is interpreted as a comparison between complete configurations rather than as an isolated component effect.

\chapter{Experimental Design}
\label{ch:experimental}

This chapter organizes the empirical design around the five research questions. Statistical definitions are given in Chapter~\ref{ch:data}; auxiliary selection configurations, detailed paired statistics, and exact model-scale values are reported in Appendix~\ref{app:supporting}.

\section{Evaluation Overview}
\label{sec:experimental-overview}

The evaluation first positions HiED against same-split baselines, then localizes disagreement across candidate detection, criterion verification, and primary selection. Primary-selection interventions test possible remedies, while model-scale and external analyses examine robustness and transfer. Figure~\ref{fig:evidence-sequence} summarizes this argument. The ongoing psychiatrist case review is shown separately because it provides complementary interpretation rather than another completed computational research question.

\ThesisFigure[0.98\textwidth]{fig_evidence_sequence.pdf}{Overview of the empirical evidence sequence. Benchmark comparisons establish predictive positioning, stage-wise analysis localizes disagreement, selection interventions test possible remedies, and robustness analyses examine transfer across model scales and synthetic corpora. The psychiatrist case review is an ongoing complementary study based on selected disagreement cases.}{fig:evidence-sequence}

Chapters~\ref{ch:results} and~\ref{ch:external} report the internal and external computational results, respectively, while Chapter~\ref{ch:error} reports the detailed failure analysis and the psychiatrist case-review results when complete.

\section{Internal Benchmark and Retrieval Comparisons}

The public validation split selects retrieval separately for Single and HiED--DA using the predefined promotion rule across their available benchmark metrics. Selected configurations are fixed before held-out evaluation, and validation results are used for configuration selection rather than as independent test estimates.

The held-out comparison includes Majority, TF--IDF + LR, Single without retrieval, the validation-selected Single configuration, HiED--DA, and HiED--NtS. All rows use the same cases, parent projection, and scoring code, although the complete systems differ in training procedure, retrieval exposure, model calls, and output contract. Top-1 is the main predictive measure; genuine Top-3 and multilabel metrics are reported only where the relevant output view exists.

The Majority baseline predicts the most frequent training-corpus parent. TF--IDF + LR is trained on the 13,000 training cases using character-boundary 1--2-grams, at most 10,000 features, \texttt{min\_df=2}, \texttt{max\_df=0.95}, and sublinear term frequency. Twelve one-vs-rest logistic-regression classifiers use $C=1.0$, the L-BFGS solver, balanced class weights, and a 2,000-iteration limit; their probabilities define the ranking and thresholded label set. Single uses one diagnostic LLM call and emits a primary with an optional secondary diagnosis, so genuine Top-3 is undefined.

Retrieval is compared within Single and HiED--DA under no retrieval, global Top-5 retrieval, and parent-balanced retrieval. Within each architecture, the model, prompt family, diagnostic scope, decoding settings, and output contract remain fixed. Global and parent-balanced retrieval also differ in example count, composition, order, and prompt length, so their contrast evaluates complete retrieval configurations rather than the isolated effect of balancing.

\section{Stage-Wise Failure Analysis}

The stage-wise analysis uses the indicators defined in Chapter~\ref{ch:data} on one recorded HiED--DA trace: $D_3$ measures gold-label presence in the genuine Top-3, $R$ measures gold-label retention after criterion verification, and $S$ measures agreement of the committed primary with the gold-label set. $D_3$ and $S$ are available on all internal cases, whereas $R$ is restricted to checker-eligible cases.

The joint $D_3$--$R$--$S$ analysis therefore uses the checker-eligible subset and reports the corresponding conditional rates. Its strict primary-selection subset satisfies $D_3=1$, $R=1$, and $S=0$: a gold parent is ranked and retained but not selected as the committed primary. The indicators are not a strictly nested runtime funnel because criterion checking covers the configured scope rather than only the genuine Top-3.

A supporting oracle analysis searches a broader window formed from ranked and criterion-compatible diagnoses and identifies cases in which a gold parent appears in a similarly supported cluster. Because it uses the gold label and assumes identified errors can be corrected without harming correct cases, it is interpreted only as an optimistic, non-deployable upper bound.

\section{Primary-Selection Strategies}

Committed Top-1 agreement is the main comparison measure. DA preserves the Diagnostician's primary, whereas NtS reuses the recorded ranking, checker states, and criterion-compatible set to select one diagnosis without new LLM inference. DA and NtS are compared on identical cases within all three retrieval conditions, and difference-in-differences bootstrap contrasts test whether retrieval changes the finalization-policy effect. Because NtS emits one label, its set-level metrics also reflect output cardinality and do not isolate primary re-selection.

Recorded-output methods such as NtS and deterministic fusion reuse DA artifacts and permit relatively direct attribution to the tested selection rule. Independent pairwise comparison, forced pairwise application, debate, SC, and CISC require new inference or sampling and are evaluated as complete configurations. Their execution settings and interpretation boundaries are summarized in Appendix~\ref{app:supporting}.

\section{Robustness and External Synthetic Evaluation}

Model-scale sensitivity is evaluated across Qwen3-4B, 8B, 14B, and 32B on the same internal cases with a common prompt family, scope, retrieval policy, decoding contract, and endpoint. Parsing and abstention events are recorded separately; the comparison addresses within-family scale sensitivity.

External evaluation uses the MDD-5k checker-covered full set, active-profile in-scope subset, and restricted F32/F41 subset. The common DA trace supports multi-way performance, the external Top-3--Top-1 selection gap, and common-output primary-selection comparisons. A matched external criterion-retention trace is unavailable, so the full internal $D_3$--$R$--$S$ analysis is not repeated.

Additional transfer analyses expand the configured diagnostic scope without changing model weights and evaluate lexical classification, BGE-M3 case-neighbor label voting, and score fusion across corpora. These experiments distinguish output-space representability from behavioral recovery and same-corpus fit from selector portability.

\section{Psychiatrist Case Review}
\label{sec:psychiatrist-case-review}

At the time of this manuscript revision, the psychiatrist case review was ongoing. Preliminary judgments are excluded from Chapters~\ref{ch:results} and~\ref{ch:external}; completed reviewer characteristics, outcomes, and agreement estimates will be reported in Chapter~\ref{ch:error}.

The primary review population is sampled from checker-eligible internal cases satisfying $D_3=1$, $R=1$, and $S=0$. Sampling does not use preliminary judgments, and secondary comparison strata are reported separately. The completed report will state sample sizes, exclusions, replacement rules, reviewer qualifications, clinical experience, language competence, and prior exposure to the benchmark or system.

Reviewers first receive only the transcript and configured diagnostic scope. Gold labels, system outputs, the $D_3$--$R$--$S$ pattern, and other reviewers' judgments remain concealed while reviewers record a preferred primary diagnosis, plausible alternatives, unique-primary determinability, and missing information. These ratings are locked before system outputs are shown.

Reviewers then inspect the ranked differential, criterion states, evidence spans, criterion-compatible candidates, and committed primary while the gold label remains concealed. After these ratings are locked, the gold label is revealed and reviewers classify the disagreement as a primary-selection error, insufficient transcript information, multiple plausible diagnoses, gold-label ambiguity, criterion-verification error, diagnostic-scope limitation, or unresolved.

The primary outcome is the proportion of reviewed ranked-and-retained nonselection cases for which the prespecified psychiatrist-consensus rule identifies a gold diagnosis as the preferred primary:
\[
\widehat{\pi}_{\mathrm{clinical}}
=
\frac{n_{\mathrm{gold\ preferred}}}
{n_{D_3=1,\,R=1,\,S=0\ \mathrm{reviewed}}}.
\]
Secondary measures include gold-diagnosis plausibility, transcript sufficiency, plausibility of the committed diagnosis, disagreement classification, audit-trace usefulness, and inter-rater agreement for the principal review judgments.

Together, these designs distinguish benchmark positioning, stage-wise localization, intervention evidence, transfer, and complementary clinical interpretation without treating them as equivalent claims.

\chapter{In-Domain Evaluation Results}
\label{ch:results}

This chapter first establishes the predictive position of HiED on the internal evaluation set, then localizes disagreements across candidate detection, criterion verification, and primary selection. Retrieval, primary-selection strategies, model scale, and diagnostic confusions are examined afterward as alternative explanations and supporting analyses.

\section{Configuration Selection and Main Internal Performance}
\label{sec:validation-retrieval-selection}
\label{sec:matched-baselines-retrieval}

Table~\ref{tab:validation-configuration-matrix} reports all applicable metrics for the retained public-validation configurations together with three representative results from the original LingxiDiagBench static evaluation. The Single rows do not report Top-3 because Single does not emit a genuine ordered differential. A matched HiED--DA no-retrieval validation result was not retained; the unavailable row is shown explicitly so that absence of evidence is not confused with an evaluated zero or with the internal-test no-retrieval control reported in Section~\ref{sec:retrieval-results}. The published rows provide benchmark context rather than matched comparisons.

\begin{table}[H]
\centering
\caption{Public-validation configuration results and published LingxiDiagBench context for the 12-category diagnostic task. Published rows are included for descriptive context only because their prompts, inference procedures, and output contracts differ from those used in this thesis~\citep{xu2026lingxidiagbench}.}
\label{tab:validation-configuration-matrix}
\small
\begin{adjustbox}{max width=\textwidth}
\begin{tabular}{|l|l|c|c|c|c|c|}
\hline
Method & Retrieval or source & Top-1 & Top-3 & Exact Match & Macro-F1 & Weighted-F1\\
\hline
\multicolumn{7}{|c|}{\textbf{Thesis validation configurations}}\\
\hline
Single & None & 0.466 & --- & 0.016 & 0.174 & 0.429\\
Single & Global Top-5 & 0.519 & --- & 0.030 & 0.209 & 0.456\\
Single & Parent-balanced & 0.507 & --- & 0.016 & 0.195 & 0.437\\
\hline
HiED--DA & None & --- & --- & --- & --- & ---\\
HiED--DA & Global Top-5 & 0.525 & 0.797 & 0.432 & 0.178 & 0.435\\
HiED--DA & Parent-balanced & 0.523 & 0.800 & 0.428 & 0.181 & 0.440\\
\hline
\multicolumn{7}{|c|}{\textbf{Published LingxiDiagBench static results}}\\
\hline
TF--IDF + LR & Published benchmark & 0.496 & 0.645 & 0.268 & 0.295 & 0.520\\
GPT-5-Mini & Published benchmark & 0.487 & 0.505 & 0.409 & 0.188 & 0.418\\
Qwen3-32B & Published benchmark & 0.470 & 0.566 & 0.241 & 0.188 & 0.431\\
\hline
\end{tabular}
\end{adjustbox}
\parbox{0.96\textwidth}{\footnotesize \textit{Note:} Dashes in the Single Top-3 cells denote an undefined output view; dashes across the HiED--DA no-retrieval row denote an unavailable matched validation result. The published rows use the original benchmark prompts and evaluation pipeline and are not treated as matched system comparisons. Their reported 12-category accuracy is shown as Exact Match because it requires agreement with the complete gold-label set under the original multilabel evaluator.}
\end{table}

Global Top-5 improves every native reported endpoint for Single relative to parent-balanced retrieval and is used as the primary Single configuration. For HiED--DA, global Top-5 improves Top-1 Accuracy and Exact Match relative to parent-balanced retrieval, but reduces genuine Top-3 Accuracy, Macro-F1, and Weighted-F1. It therefore improves only two of the five predefined promotion metrics and does not satisfy the promotion rule; parent-balanced retrieval remains the primary HiED configuration. The HiED Top-1 difference is 0.002 (95\% CI [$-0.013$, 0.017]), so the interval includes zero. Detailed discordant counts and the exact test are reported in Appendix~\ref{app:supporting}, Table~\ref{tab:internal-paired-tests}.

The published rows provide benchmark context rather than matched baselines. Under the original LingxiDiagBench evaluation contract, TF--IDF + LR achieved the strongest Top-3 and label-level F1 results among these representative rows, whereas GPT-5-Mini achieved the highest 12-category Exact Match. Qwen3-32B provides context for the backbone used in the primary HiED configuration. No direct statistical comparison is made because the published systems use different prompts, output structures, retrieval conditions, and scoring procedures. The published results nevertheless show that ranked candidate coverage exceeded primary prediction in the source benchmark; the present thesis extends that observation by separating candidate detection, criterion verification, and primary selection.

Table~\ref{tab:matched-baselines-retrieval} reports the main comparison on the held-out internal cases.

\begin{table}[H]
\centering
\caption{Main same-split system comparison on the fixed 1,000-case internal test set. Single and HiED use retrieval configurations selected separately on the public validation split. Top-3 is reported only for systems with a genuine ranked output.}
\label{tab:matched-baselines-retrieval}
\scriptsize
\begin{adjustbox}{max width=\textwidth}
\begin{tabular}{|l|l|p{4.0cm}|c|c|c|c|c|}
\hline
System & Retrieval & Finalization or output contract & Top-1 & Top-3 & Exact Match & Macro-F1 & Weighted-F1\\
\hline
Majority & None & Single label & 0.398 & --- & 0.327 & 0.047 & 0.207\\
\hline
TF--IDF + LR & None & Ranked classifier output & 0.632 & 0.923 & 0.321 & 0.393 & 0.601\\
\hline
Single & Global Top-5 & Primary and optional secondary & 0.517 & --- & 0.029 & 0.224 & 0.447\\
\hline
HiED--DA & Parent-balanced & Ranked output and audit trace & 0.518 & 0.802 & 0.409 & 0.178 & 0.434\\
\hline
HiED--NtS$^{\dagger}$ & Parent-balanced & Single-label re-selection & 0.490 & 0.802 & 0.437 & 0.181 & 0.408\\
\hline
\end{tabular}
\end{adjustbox}
\parbox{0.96\textwidth}{\footnotesize $^{\dagger}$NtS differs from DA in finalization and emitted-set cardinality. It is included for system-level context but is not treated as a component-matched architecture comparison.}
\end{table}

All rows use the same cases, parent-label projection, and scoring implementation, but differ in training procedure, model calls, retrieval exposure, and output contract. TF--IDF + logistic regression is the strongest same-split predictor. The Top-1 difference for HiED--DA relative to TF--IDF is $-0.114$ (95\% CI [$-0.150$, $-0.079$]). HiED is therefore not an accuracy-leading system on this benchmark; its additional empirical value lies in the ranked differential, criterion-level audit, and stage-wise observability. Detailed paired-test results are reported in Appendix~\ref{app:supporting}, Table~\ref{tab:internal-paired-tests}.

The lexical result also counters an exclusively missing-information explanation: transcript surface regularities recover some benchmark labels more effectively than the evaluated LLM pipeline. Synthetic generation may create lexical shortcuts, so this same-split result does not establish clinical or cross-corpus superiority. It does show that residual HiED errors alone cannot establish that the transcripts contain no usable primary-diagnosis signal.

\section{Retrieval Results}
\label{sec:retrieval-results}

\begin{table}[htbp]
\centering
\caption{Retrieval-strategy results on the fixed internal test set. Single has no genuine ranked output, so Top-3 is not reported. The table presents test-set ablations and does not alter the validation-selected main configurations.}
\label{tab:retrieval-results}
\begin{adjustbox}{max width=\textwidth}
\begin{tabular}{|l|l|c|c|c|c|c|}
\hline
Architecture & Retrieval & Top-1 & Top-3 & Exact Match & Macro-F1 & Weighted-F1\\
\hline
Single & None & 0.466 & --- & 0.017 & 0.183 & 0.426\\
Single & Global Top-5 & 0.517 & --- & 0.029 & 0.224 & 0.447\\
Single & Parent-balanced & 0.508 & --- & 0.011 & 0.192 & 0.433\\
\hline
HiED--DA & None & 0.510 & 0.786 & 0.404 & 0.174 & 0.437\\
HiED--DA & Global Top-5 & 0.524 & 0.815 & 0.435 & 0.196 & 0.437\\
HiED--DA & Parent-balanced & 0.518 & 0.802 & 0.409 & 0.178 & 0.434\\
\hline
\end{tabular}
\end{adjustbox}
\end{table}

Global Top-5 produces the strongest test-set point estimates for Single. Both retrieval conditions improve Single Top-1 over no retrieval after within-family Holm adjustment, while global and parent-balanced retrieval do not differ significantly. Global Top-5 remains the selected Single configuration.

For HiED--DA, global retrieval has the highest Top-1, Top-3, Exact Match, and Macro-F1 point estimates, but none of the paired Top-1 contrasts is statistically supported after adjustment. Parent-balanced retrieval therefore remains the validation-selected main configuration. Because global and parent-balanced retrieval differ in example count, composition, order, and prompt length, this table compares complete retrieval configurations rather than isolating balancing as the cause.

\section{Stage-Wise Failure Analysis}
\label{sec:stage-wise-failure-results}

\paragraph{Candidate coverage.}

HiED--DA achieves a genuine Top-3 Accuracy of 0.802 and a committed Top-1 Accuracy of 0.518 on the same 1,000 test cases. Under the DA contract, the emitted primary must be selected from the Diagnostician's genuine Top-3, so every Top-1-correct case is also Top-3 correct. The 0.284 difference corresponds to 284 cases in which a benchmark gold parent appears in the genuine Top-3 but is not selected as the committed primary.

\ThesisFigure[0.98\textwidth]{fig_b_selection_bottleneck.pdf}{Committed Top-1 agreement and genuine Top-3 gold-label coverage on the 1,000-case internal test set. The 284-case difference is directly observed under the DA output contract. The separately boxed 349-case oracle analysis uses a broader, gold-informed candidate window and yields a deliberately optimistic, non-deployable upper bound.}{fig:selection-bottleneck}

Candidate generation alone therefore cannot explain all benchmark disagreements because the gold label is already present among the leading candidates in these cases. The difference quantifies selection headroom under benchmark scoring; it does not establish that all 284 cases contain sufficient clinical evidence for a unique primary diagnosis or are recoverable by another finalization rule.

\paragraph{Criterion verification.}
\label{sec:criterion-verification}

Among the 915 cases with a non-\emph{Others} gold parent and a corresponding checker, the criterion-compatible set contains a gold parent in 857 cases (0.937; 95\% CI [0.919, 0.951]). The median compatible set contains six of the fourteen configured diagnostic categories. This shows high gold-label retention but limited selectivity.

Criterion-state distributions are analyzed by failure type in Chapter~\ref{ch:error}. Those states are model-generated audit outputs rather than clinician-validated criterion labels.

\paragraph{Joint failure localization.}

Table~\ref{tab:d3rs-joint} reports the case-level joint distribution of candidate detection, gold-label retention after criterion verification, and primary-selection correctness. ``Yes'' means that at least one benchmark gold parent satisfies the stated condition; ``No'' means that none does. The table is restricted to the 915 checker-eligible cases for which all three indicators are defined, so cases without a corresponding checker are excluded rather than coded as $R=0$. The indicators are not strictly nested: all fourteen configured diagnostic categories are checked, so a gold parent may be retained in the criterion-compatible set even when it is absent from the Diagnostician's genuine Top-3.

\begin{table}[htbp]
\centering
\caption{Joint distribution of candidate detection ($D_3$), gold-label retention after criterion verification ($R$), and primary-selection correctness ($S$) on the 915 checker-eligible cases.}
\label{tab:d3rs-joint}
\small
\setlength{\tabcolsep}{3pt}
\begin{tabularx}{\textwidth}{|>{\centering\arraybackslash}p{2.3cm}|>{\centering\arraybackslash}p{2.9cm}|>{\centering\arraybackslash}p{2.5cm}|>{\centering\arraybackslash}p{2.1cm}|X|}
\hline
Gold label in genuine Top-3 ($D_3$)? & Gold label in criterion-compatible set ($R$)? & Committed primary correct ($S$)? & Cases, $n$ (\%) & Interpretation\\
\hline
No & No & No & 34 (3.7\%) & Neither detected nor retained; primary incorrect\\
No & Yes & No & 79 (8.6\%) & Retained by full-scope checking but absent from Top-3\\
Yes & No & No & 12 (1.3\%) & Detected but not retained; primary incorrect\\
Yes & No & Yes & 12 (1.3\%) & Correctly selected despite non-retention\\
Yes & Yes & No & 272 (29.7\%) & Ranked and retained but not selected\\
Yes & Yes & Yes & 506 (55.3\%) & Ranked, retained, and correctly selected\\
\hline
\end{tabularx}
\parbox{0.96\textwidth}{\footnotesize \textit{Note:} Profiles with $D_3=0$ and $S=1$ are impossible under the DA ranking contract and are omitted. Percentages are shares of the 915 checker-eligible cases and may not sum to exactly 100\% because of rounding.}
\end{table}

\ThesisFigure[0.98\textwidth]{fig_d3rs_flow.pdf}{Joint detection--verification--selection profiles on the 915 checker-eligible internal cases. The criterion-compatible set is evaluated over the configured checker scope rather than only the Top-3, so the paths summarize joint case profiles rather than a strictly nested runtime funnel.}{fig:d3rs-flow}

The joint analysis localizes the remaining gap on the same 915 cases. Among the 802 cases with a gold label in the Top-3, 778 also retain a gold label after criterion verification, giving $P(R\mid D_3)=0.9701$. Across all 857 retained cases, 506 select a gold label as the committed primary, giving $P(S\mid R)=0.5904$. Among cases with both detection and retention, $P(S\mid D_3,R)=0.6504$, leaving 272 cases in which a gold label is ranked and retained but not selected. Once a gold label is detected, retention after verification is common; the larger remaining loss occurs during primary selection among retained alternatives. These are benchmark-condition quantities derived from algorithmic checker states, not clinician-validated probabilities.

Table~\ref{tab:gap-counts} separates the three counts used in the failure-localization argument. They answer different questions and cannot be substituted for one another.

\begin{table}[htbp]
\centering
\caption{Distinct case counts used in the ranking, retention, and oracle analyses.}
\label{tab:gap-counts}
\small
\begin{tabularx}{\textwidth}{|r|p{5.2cm}|X|}
\hline
Cases & Operational definition & Evidentiary interpretation\\
\hline
284 & $D_3=1$ and $S=0$ on all 1,000 internal cases & Gold parent ranked in the genuine Top-3 but not selected as the committed primary\\
\hline
272 & $D_3=1$, $R=1$, and $S=0$ on 915 checker-eligible cases & Ranked-and-retained but not selected subset\\
\hline
349 & Gold parent appears in a broader supported cluster among misclassified cases & Gold-informed oracle upper-bound cases; not deployable recovery\\
\hline
\end{tabularx}
\end{table}

\paragraph{Oracle analysis.}

This analysis is not used as primary evidence for the selection bottleneck. A broad gold-label-in-supported-cluster oracle identifies 349 misclassified cases whose gold parent appears in any similarly supported cluster. It does not require the DA primary and gold parent to occur in the same cluster, uses the gold label to identify eligible cases, and assumes that every identified error can be corrected without harming an originally correct case. The resulting 0.867 upper bound is therefore deliberately optimistic rather than a deployable selector or guaranteed recovery level.

\section{Primary-Selection Strategy Results}
\label{sec:nts-factorial}
\label{sec:additional-selection-results}

Table~\ref{tab:nts-factorial} compares DA and NtS on the same 1,000 cases within each retrieval condition. Differences are NtS minus DA.

\begin{table}[htbp]
\centering
\caption{Paired comparison of Direct-Answer (DA) and Nominate-then-Select (NtS) under three retrieval conditions on the internal test split ($N=1000$ per condition). Confidence intervals are paired-bootstrap intervals; negative differences favor DA.}
\label{tab:nts-factorial}
\small
\begin{tabular}{|l|c|c|c|}
\hline
Retrieval condition & DA Top-1 & NtS Top-1 & Difference (95\% CI), pp\\
\hline
No retrieval & 51.0\% & 48.2\% & $-2.8$ [$-4.1$, $-1.5$]\\
\hline
Global Top-5 & 52.4\% & 49.5\% & $-2.9$ [$-4.4$, $-1.4$]\\
\hline
Parent-balanced & 51.8\% & 49.0\% & $-2.8$ [$-4.3$, $-1.3$]\\
\hline
\end{tabular}
\end{table}

NtS reduces Top-1 by 2.8--2.9 percentage points under all three retrieval conditions, and every confidence interval excludes zero. The prespecified interaction contrasts are at most 0.1 percentage points and their intervals span zero, providing no evidence that retrieval moderates the NtS penalty; this is not an equivalence result. Because NtS emits one diagnosis while DA may emit a secondary diagnosis, set-level metric changes also reflect output cardinality. Detailed discordant counts, interaction intervals, and adjusted tests are reported in Appendix~\ref{app:supporting}, Table~\ref{tab:internal-paired-tests}.

Table~\ref{tab:internal-selection-results} summarizes the additional methods. Recorded-output methods permit cleaner attribution, whereas pairwise and debate rows compare complete new-inference configurations.

\begin{table}[htbp]
\centering
\caption{Additional internal primary-selection methods relative to DA. Confidence intervals are paired-bootstrap intervals for the Top-1 difference; a dash denotes unavailable paired records.}
\label{tab:internal-selection-results}
\small
\begin{tabularx}{\textwidth}{|>{\raggedright\arraybackslash}p{3.2cm}|c|c|c|X|}
\hline
Method & Top-1 & $\Delta$ vs DA & 95\% CI & Interpretation\\
\hline
DA & 0.518 & --- & --- & Reference output\\
\hline
Deterministic fusion & 0.514 & $-0.004$ & --- & Recorded-output summary\\
Forced pairwise override & 0.443 & $-0.075$ & [$-0.098$, $-0.053$] & Complete configuration\\
Pairwise independent & 0.524 & $+0.006$ & [$-0.010$, 0.022] & Complete configuration\\
Two-view debate & 0.485 & $-0.033$ & [$-0.049$, $-0.018$] & Complete configuration\\
\hline
\end{tabularx}
\end{table}

Forced pairwise override and debate reduce Top-1, with intervals excluding zero. Independent pairwise has a small positive point estimate whose interval includes zero. Deterministic fusion lacks the paired record needed for an interval, while the conservative pairwise gate makes no changes. Internal SC and CISC remain descriptive because only summary-level records were retained. None of the evaluated methods provides a clear and attributable improvement over DA; detailed discordant counts and exact tests are reported in Appendix~\ref{app:supporting}.

\section{Model-Scale and Diagnostic Error Analysis}
\label{sec:robustness-error-distribution}

\paragraph{Model scale.}

\ThesisFigure{fig_a_size_robustness.pdf}{Held-out Top-1 across Qwen3 model sizes for the Single baseline and HiED.}{fig:size-robustness}

Single Top-1 increases monotonically across the evaluated sizes, whereas HiED--DA varies over a narrower range and genuine Top-3 remains comparatively stable. The 4B run also records parsing and abstention events. This pattern shows limited sensitivity across the evaluated Qwen3 AWQ sizes; it is not evidence of robustness across unrelated model families. Exact values and run-integrity notes are reported in Appendix~\ref{app:supporting}, Table~\ref{tab:qwen-size-results}.

\paragraph{Diagnostic confusions.}

The first-listed-label confusion analysis has agreement 0.509, distinct from the any-gold Top-1 Accuracy of 0.518. Figure~\ref{fig:confusion-thesis-rewrite} uses one first-listed gold parent per case for descriptive visualization; it is not the multilabel primary endpoint.

\ThesisFigure{fig_d_confusion_heatmap.pdf}{Confusion matrix on the internal test set using the first-listed gold parent for each case.}{fig:confusion-thesis-rewrite}

The largest confusion is F41$\rightarrow$F32 (178 cases), compared with 24 in the reverse direction. Other frequent pairs are F39$\rightarrow$F32 (43), \emph{Others}$\rightarrow$F32 (37), and \emph{Others}$\rightarrow$F41 (26). F32 is therefore the most frequent destination among the major pairs. The matrix establishes this prediction asymmetry but does not determine whether it results from class frequency, lexical cues, missing temporal evidence, or model preference. Chapter~\ref{ch:error} uses the audit trace to localize the corresponding failure patterns.

Overall, TF--IDF + logistic regression is the strongest same-split predictor, while HiED exposes a measurable gap between candidate coverage, criterion-based retention, and primary selection. Retrieval improves Single more clearly than HiED; NtS lowers Top-1 under all three retrieval conditions; and no additional strategy provides a clear attributable gain. Chapter~\ref{ch:external} examines which parts of this pattern remain observable under external synthetic distribution shift.


\chapter{External Synthetic Evaluation}
\label{ch:external}

This chapter tests which in-domain findings remain observable on MDD-5k, a second Chinese synthetic corpus with different generation procedures, label distributions, and diagnostic coverage. Its analysis populations are subsets of one source corpus rather than independent cohorts, and the results do not constitute real-patient or Taiwanese clinical validation.

\section{External Performance and Selection Gap}
\label{sec:mdd-multiway}
\label{sec:external-ranking-gap}

Table~\ref{tab:external-inventory} distinguishes the populations used for performance, scope, and transfer analyses. Their results are not directly comparable because the sampling, label spaces, and endpoints differ.

\begin{table}[htbp]
\centering
\caption{External analysis populations and their roles.}
\label{tab:external-inventory}
\small
\begin{tabularx}{\textwidth}{|X|r|X|}
\hline
Analysis population & $N$ & Role\\
\hline
Checker-covered full set & 925 & Multi-way performance and common-trace analyses\\
In-scope subset & 878 & Performance when a gold diagnosis is representable\\
Out-of-scope cases & 47 & Diagnostic-scope limitation\\
Restricted F32/F41 subset & 490 & Separate binary task\\
Lexical-transfer holdout & 185 & Corpus-portability analysis\\
Scope-expansion sample & 58 & Newly representable diagnoses\\
\hline
\end{tabularx}
\end{table}

\begin{table}[htbp]
\centering
\caption{External predictive performance. Panel A reports multi-way evaluation; Panel B reports the separate restricted F32/F41 task.}
\label{tab:external-summary-rewrite}
\small
\textbf{Panel A: Multi-way evaluation}\\[3pt]
\setlength{\tabcolsep}{3.5pt}
\begin{tabularx}{\textwidth}{|X|r|r|r|r|r|r|r|}
\hline
Evaluation set & $N$ & Top-1 & Top-3 & Exact Match & Macro-F1 & Weighted-F1 & \shortstack{Majority\\Top-1}\\
\hline
MDD-5k full & 925 & 0.5708 & 0.8508 & 0.3881 & 0.2101 & 0.5188 & 0.4432\\
MDD-5k in scope & 878 & 0.6014 & 0.8964 & 0.4089 & 0.2488 & 0.5609 & 0.4670\\
\hline
\end{tabularx}
\vspace{7pt}

\textbf{Panel B: Restricted F32/F41 evaluation}\\[3pt]
\begin{tabular}{|l|r|r|r|r|r|r|r|}
\hline
Binary subset & $N$ & Accuracy & \shortstack{Majority\\accuracy} & Macro-F1 & Weighted-F1 & F32 F1 & F41 F1\\
\hline
MDD pure F32/F41 & 490 & 0.9082 & 0.7837 & 0.8814 & 0.9163 & 0.9429 & 0.8200\\
\hline
\end{tabular}
\end{table}

HiED exceeds the corresponding majority baseline on both multi-way views. Excluding the 47 cases whose gold diagnoses are outside the configured output space raises all system metrics, while Weighted-F1 remains well above Macro-F1, indicating the influence of frequent labels.

The restricted task is substantially easier than multi-way diagnosis. Its 384 F32 and 106 F41 cases yield stronger F32 than F41 F1, so the 0.9082 accuracy is interpreted as a separate coarse diagnostic contrast rather than overall external performance.

On the full set, genuine Top-3 is 0.8508 and committed Top-1 is 0.5708. The 0.2800 difference corresponds to 259 cases in which a gold parent is ranked but not selected.

\ThesisFigure[0.92\textwidth]{fig_internal_external_gap.pdf}{Committed Top-1 and genuine Top-3 gold-label agreement on the internal and MDD-5k synthetic corpora. The figure compares Top-3--Top-1 selection gaps only; no external criterion-retention measure is available.}{fig:internal-external-gap}

This external Top-3--Top-1 gap is consistent with the in-domain pattern, but the common trace does not contain a matched criterion-retention view. The full external $D_3$--$R$--$S$ analysis therefore cannot be reconstructed.

\section{External Primary-Selection Strategies}
\label{sec:mdd-retrieval-finalization}

The planned six-row external retrieval comparison was not available as a validated matched bundle, so retrieval-policy transfer remains unresolved and no values from older unmatched runs are substituted. Table~\ref{tab:mdd-finalization-results} reports the available paired primary-selection comparisons against the common DA trace.

\begin{table}[htbp]
\centering
\caption{External primary-selection strategies relative to DA on 925 matched cases. Differences and paired-bootstrap confidence intervals are in percentage points.}
\label{tab:mdd-finalization-results}
\small
\begin{tabularx}{\textwidth}{|>{\raggedright\arraybackslash}p{4.2cm}|>{\raggedright\arraybackslash}p{2.8cm}|c|X|}
\hline
Method & Contract & Top-1 & Difference vs DA (95\% CI), pp\\
\hline
Direct-Answer & Reference output & 57.08\% & ---\\
Nominate-then-Select & Recorded output & 40.65\% & $-16.43$ [$-19.5$, $-13.5$]\\
Deterministic fusion & Recorded output & 47.14\% & $-9.95$ [$-12.4$, $-7.5$]\\
Two-view debate & New inference & 37.84\% & $-19.24$ [$-22.6$, $-16.0$]\\
Self-consistency ($K=3$) & Sampling & 57.84\% & $+0.76$ [$-0.9$, 2.4]\\
Self-consistency ($K=5$) & Sampling & 58.16\% & $+1.08$ [$-0.5$, 2.7]\\
Confidence-informed SC ($K=3$) & Sampling & 57.84\% & $+0.76$ [$-0.9$, 2.4]\\
Confidence-informed SC ($K=5$) & Sampling & 58.16\% & $+1.08$ [$-0.5$, 2.7]\\
\hline
\end{tabularx}
\end{table}

NtS and deterministic fusion reuse recorded rank and criterion signals, yet both reduce Top-1 and their confidence intervals exclude zero. A conservative pairwise gate makes no changes. The complete debate configuration also performs below DA, but its larger context budget and additional calls prevent attribution to debate alone.

Sampling-based aggregation produces small positive point estimates whose intervals include zero. No evaluated strategy provides a clear reliable external gain. Discordant counts and exact tests are reported in Appendix~\ref{app:supporting}, Table~\ref{tab:external-paired-tests}; set-level metrics are not used to isolate NtS because it emits only one diagnosis.

\section{Diagnostic Scope and Cross-Corpus Transfer}
\label{sec:scope-coverage}

The 47 out-of-scope cases remain part of full-corpus performance because the configured system cannot emit their gold diagnoses. A configuration-only expansion tests whether making additional diagnoses representable changes committed predictions.

\begin{table}[htbp]
\centering
\caption{Diagnostic-scope expansion and committed recovery on newly representable cases.}
\label{tab:scope-expansion-results}
\small
\begin{tabularx}{\textwidth}{|p{2.4cm}|c|c|X|c|}
\hline
Configuration & Categories & Scoring parents & Newly representable cases & \shortstack{Top-1\\recovery}\\
\hline
Original & 14 & 11 & --- & ---\\
Expanded & 33 & 30 & 58 & 0/58\\
\hline
\end{tabularx}
\end{table}

The expanded profile executes with 33 categories and 30 scoring parents, but none of the 58 newly representable gold parents becomes Top-1. Configuration expansion therefore increases representability without demonstrating behavioral recovery. No paired in-scope preservation result is claimed for this sample.

\begin{table}[H]
\centering
\caption{Within-corpus and cross-corpus TF--IDF performance. The MDD cells use the same 185-case holdout.}
\label{tab:lexical-transfer-rewrite}
\begin{tabular}{|l|l|r|c|l|}
\hline
Training corpus & Evaluation corpus & $N$ & Top-1 & Interpretation\\
\hline
LingxiDiag & LingxiDiag & 1000 & 0.610 & within-corpus fit\\
LingxiDiag & MDD-5k & 185 & 0.049 & cross-corpus degradation\\
MDD-5k & LingxiDiag & 1000 & 0.485 & reverse degradation\\
MDD-5k & MDD-5k & 185 & 0.692 & within-MDD fit\\
\hline
\end{tabular}
\end{table}

The 0.610 LingxiDiag row belongs to a development/transfer lineage and is distinct from the 0.632 matched internal baseline. The same classifier family fits each source corpus much better than it transfers, indicating corpus-specific wording and label-prior dependence rather than portable diagnostic reasoning.

Supporting BGE-M3 analyses examine the same boundary from case similarity. Standalone neighbor-label voting is a separate source-corpus selector, while score fusion modifies recorded HiED scores. These representation-specific analyses use a DA trace different from the unified main external analysis and remain repository-level supporting evidence.

Overall, MDD-5k reproduces the Top-3--Top-1 gap and the absence of a reliable gain from the evaluated selection strategies, but not the complete internal verification pathway. Scope expansion improves representability without recovery, and lexical and case-neighbor signals transfer poorly across corpora. Chapter~\ref{ch:error} examines the corresponding failure patterns without assigning clinical causes.


\chapter{Stage-Wise Failure Analysis}
\label{ch:error}

This chapter analyzes disagreements between model outputs and benchmark gold labels. The primary analysis uses the parent-balanced HiED--DA trace on the fixed 1,000-case internal test set because complete ranking, checker, compatibility, and finalization artifacts are available for that run. Candidate-detection and diagnostic-set analyses use all 1,000 cases. Analyses involving gold-label retention use the 915 checker-eligible cases, and external diagnostic-scope findings use separately identified MDD-5k populations.

The term \emph{failure} denotes an operational benchmark disagreement at a recorded system stage. It does not establish a psychiatrist-confirmed diagnostic error or its clinical cause. The stages are also not a strict runtime funnel because criterion verification covers all configured diagnostic categories rather than only the genuine Top-3. Denominators and operational conditions are therefore stated in each section.

\section{Failures before Primary Selection}

\paragraph{Candidate-detection failures.}

A candidate-detection failure occurs when no benchmark gold parent appears in the genuine Top-3, or $D_3=0$. This condition occurs in 198 of the 1,000 internal cases. Eighty-five of these cases use \emph{Others} as the gold label and are not eligible for gold-label retention analysis, leaving 113 checker-eligible candidate-detection failures.

The full-scope checker retains a gold parent in 79 of these 113 cases despite its absence from the Diagnostician's Top-3; the remaining 34 cases neither rank nor retain a gold parent. This non-nested profile shows that candidate generation and criterion verification expose different recorded signals. Across all 198 candidate-detection failures, the committed primary is concentrated in F32 (109 cases) and F41 (56 cases), but these benchmark counts do not determine whether the omitted gold diagnosis was clinically preferable.

\paragraph{Criterion-verification failures.}

A criterion-verification failure occurs when a gold parent appears in the genuine Top-3 but is not retained in the criterion-compatible set, or $D_3=1$ and $R=0$. Twenty-four of the 915 checker-eligible cases meet this condition. The committed primary nevertheless matches a gold parent in twelve cases; the other twelve have an incorrect committed primary and form the criterion-verification failure group used in the mutually exclusive comparison in Section~\ref{sec:ch8-criterion-evidence}.

Across the 36 gold-label-associated checker outputs in these 24 cases, 286 criterion states are recorded: 17.1\% are \texttt{met}, 23.4\% are \texttt{not\_met}, and 59.4\% are \texttt{insufficient\_\allowbreak{}evidence}. These are criterion states rather than case counts. The high unresolved share describes the model-generated rule inputs associated with non-retention, but it does not establish that the clinical information was absent or that the checker judgment was correct.

\section{Primary-Selection Failures}

The strict primary-selection failure condition is $D_3=1$, $R=1$, and $S=0$: a gold parent is ranked and retained after criterion verification but another diagnosis is selected as the committed primary. This condition occurs in 272 of the 915 checker-eligible cases. It is narrower than the 284-case Top-3--Top-1 gap reported over all internal cases.

\ThesisFigure[0.72\textwidth]{fig_ch8_reference_rank.pdf}{Rank of the nearest benchmark gold parent among the 272 ranked-and-retained cases in which a different primary diagnosis is selected.}{fig:ch8-reference-rank}

The nearest ranked gold parent is second in 171 cases (62.9\%) and third in 101 (37.1\%). Table~\ref{tab:ch8-selection-pairs} reports the most frequent nearest-gold-to-selected-primary pairs. Each case contributes one pair, based on the highest-ranked gold parent.

\begin{table}[htbp]
\centering
\caption{Most frequent nearest-gold-to-selected-primary pairs in the 272 strict primary-selection failures.}
\label{tab:ch8-selection-pairs}
\small
\begin{tabular}{|l|l|r|r|}
\hline
Nearest gold parent & Selected primary & Cases & Share of subset\\
\hline
F41 & F32 & 158 & 58.1\%\\
F39 & F32 & 35 & 12.9\%\\
F32 & F41 & 11 & 4.0\%\\
F41 & F42 & 9 & 3.3\%\\
F51 & F32 & 9 & 3.3\%\\
\hline
\end{tabular}
\end{table}

The criterion-compatible set has a median size of six categories (interquartile range, five to six) in this subset. The same median and interquartile range occur among cases with a correct committed primary. Compatible-set size alone therefore does not distinguish correct primary selection from ranked-and-retained nonselection.

\section{Diagnostic-Set and Scope Failures}

\paragraph{Diagnostic-set failures.}

Diagnostic-set errors are separated from primary-selection failures. The internal run has 518 cases with a correct committed primary and 409 cases with exact predicted-set agreement. Consequently, 109 cases have a correct primary but an incorrect full diagnostic set. Fifty-seven contain at least one extra predicted label, 54 omit at least one gold label, and two exhibit both conditions.

This distinction is also required when interpreting finalization policies. DA may emit a primary and an optional secondary diagnosis, whereas NtS emits one diagnosis. Differences in Exact Match, Macro-F1, or Weighted-F1 between these policies therefore reflect both primary re-selection and output cardinality rather than an isolated change in primary-selection quality.

\paragraph{Diagnostic-scope failures.}

A diagnostic-scope failure occurs when the benchmark gold diagnosis is unavailable in the configured output space. The full 925-case MDD-5k evaluation contains 47 such cases. They remain part of the full external performance estimate because a system cannot select a diagnosis that it is not configured to emit.

The separate configuration-expansion experiment increases the profile from fourteen to 33 configured categories and makes 58 sampled gold diagnoses representable. None becomes the committed Top-1 diagnosis (0/58), as reported in Table~\ref{tab:scope-expansion-results}. The result distinguishes successful output-space expansion from behavioral recovery; it does not show that label-space expansion is ineffective in general.

\section{Criterion-Evidence Patterns and Illustrative Cases}
\label{sec:ch8-criterion-evidence}

Across all 77,998 recorded criterion states, 48.7\% are \texttt{met}, 23.7\% are \texttt{not\_met}, and 27.6\% are \texttt{insufficient\_\allowbreak{}evidence}. These percentages describe diagnosis-by-criterion judgments, not cases.

For stage-wise comparison, the 915 checker-eligible cases are partitioned into four mutually exclusive groups: correct primary selection ($S=1$); candidate-detection failure ($D_3=0,S=0$); criterion-verification failure with an incorrect primary ($D_3=1,R=0,S=0$); and primary-selection failure ($D_3=1,R=1,S=0$). Figure~\ref{fig:ch8-criterion-composition} reports the mean per-case composition of criterion states. Table~\ref{tab:ch8-criterion-ratios} reports medians and interquartile ranges, preserving the case as the statistical unit.

\ThesisFigure[0.98\textwidth]{fig_ch8_criterion_state_composition.pdf}{Mean per-case criterion-state composition across mutually exclusive groups on the 915 checker-eligible internal cases. Percentages are within-case state ratios averaged within each group; \texttt{met}, \texttt{not\_met}, and \texttt{insufficient\_evidence} retain the recorded output schema names.}{fig:ch8-criterion-composition}

\begin{table}[htbp]
\centering
\caption{Per-case criterion-state ratios by stage-wise group. Values are median percentages with interquartile ranges in brackets.}
\label{tab:ch8-criterion-ratios}
\small
\setlength{\tabcolsep}{4pt}
\begin{tabularx}{\textwidth}{|X|r|c|c|c|}
\hline
Group & $N$ & \texttt{met} & \texttt{not\_met} & \texttt{insufficient\_evidence}\\
\hline
Correct primary selection & 518 & 50.0 [46.2, 52.6] & 23.1 [19.2, 26.9] & 26.9 [23.1, 30.8]\\
Candidate-detection failure & 113 & 48.7 [43.6, 52.6] & 21.8 [17.9, 26.9] & 28.2 [23.1, 35.9]\\
Criterion-verification failure & 12 & 41.0 [33.0, 47.4] & 27.6 [21.5, 33.0] & 27.6 [22.8, 37.2]\\
Primary-selection failure & 272 & 50.0 [46.2, 53.8] & 21.8 [19.2, 26.9] & 26.9 [21.8, 32.1]\\
\hline
\end{tabularx}
\end{table}

The primary-selection failure group has a criterion-state composition close to the correct-selection group: their mean \texttt{met} shares are 49.9\% and 49.0\%, respectively, and their mean \texttt{insufficient\_\allowbreak{}evidence} shares are 27.1\% and 27.3\%. Aggregate criterion-state composition therefore does not provide a clear separator for these cases. The criterion-verification failure group has a lower mean \texttt{met} share and higher unresolved share, but it contains only twelve cases; this comparison is descriptive and is not treated as an inferential group effect.

\paragraph{Illustrative cases.}

Table~\ref{tab:audit-trace-examples} gives three de-identified summaries from the internal evaluation artifacts. The examples show recorded outputs only; they were not reviewed by psychiatrists and include no transcript text.

\begin{table}[htbp]
\centering
\caption{De-identified audit-trace examples illustrating distinct failure locations.}
\label{tab:audit-trace-examples}
\scriptsize
\begin{adjustbox}{max width=\textwidth}
\begin{tabular}{|l|l|l|l|l|l|}
\hline
Failure type & Gold parent & Diagnostician Top-3 & Criterion-compatible set & Committed primary & Unresolved gold-label criteria\\
\hline
Candidate-detection failure & F31 & F32, F39, F41 & F32, F39, F41, F43, F51, F98 & F32 & None recorded\\
Criterion-verification failure & F32 & F39, F32, F41 & F51, F98, Z71 & F39 & B1, C2, C5\\
Primary-selection failure & F32 & F20, F32, F41 & F20, F32, F39, F41, F43 & F20 & C2, C7\\
\hline
\end{tabular}
\end{adjustbox}
\end{table}

In offline research, gold labels permit tracing whether a diagnosis was in scope, ranked, retained, and selected. Without a revealed gold label, a reviewer can inspect the committed primary, leading alternatives, evidence spans, unresolved discriminative criteria, and finalization source. The trace may help formulate follow-up questions, but it does not determine the clinically correct diagnosis.

\section{Psychiatrist Case-Review Results}
\label{sec:psychiatrist-review-results}

The psychiatrist case review is ongoing. To avoid selective reporting, this manuscript draft does not report preliminary case-level judgments or partial agreement estimates. This section will be replaced with the completed reviewer characteristics, primary and secondary outcomes, inter-rater agreement, and case-review findings after all prespecified reviews are complete.

The stage-wise analysis identifies distinct benchmark failure locations. Candidate detection fails in 198 internal cases, while full-scope checking still retains a gold parent in 79 of the 113 checker-eligible cases in this group. Twenty-four ranked gold parents are not retained after verification, and twelve of those cases also have an incorrect primary. The largest strict subset contains 272 ranked-and-retained cases in which another diagnosis is selected, with F41-to-F32 disagreements predominating. Criterion-state composition in this subset closely resembles that of correctly selected cases, indicating that aggregate checker-state ratios alone do not resolve primacy. Diagnostic-set mismatches and diagnostic-scope failures remain separate problems, and clinical causes await completion of the psychiatrist case review.

\chapter{Discussion}
\label{ch:discussion}

The internal results first establish predictive position: TF--IDF + logistic regression is the strongest same-split predictor, while HiED provides ranked differentials, criterion-level audit artifacts, and stage-wise observability rather than benchmark accuracy leadership. Within HiED, genuine Top-3 gold-label coverage reaches 0.802 and committed Top-1 agreement reaches 0.518. A gold parent is retained in 857 of 915 checker-eligible cases, and 272 cases rank and retain a gold parent without selecting it as the committed primary.

The controlled finalization-policy comparison does not convert this gap into a gain. NtS decreases Top-1 by 2.8--2.9 percentage points across all three internal retrieval conditions, and no evaluated additional primary-selection configuration provides a clear and attributable improvement over DA. On MDD-5k, Top-3 also remains above Top-1, but the missing external criterion-retention trace prevents a complete external detection--verification--selection analysis.

These findings support a benchmark-local primary-selection bottleneck in the evaluated system. They do not establish that every disagreement is a clinically verified selection error, that transcript-only diagnosis is intrinsically underdetermined, or that the pattern generalizes to patients. The ongoing psychiatrist case review is not used in the substantive conclusions until its prespecified assessments are complete.

\section{Why Detection and Verification Are Insufficient}

\paragraph{Candidate detection.}

Candidate generation remains necessary because a selector cannot choose a diagnosis that is absent from the ranked differential. The 198 internal candidate-detection failures therefore identify a direct target for improved ranking. Retrieval supports this stage in the Single architecture: both retrieval configurations improve Single relative to no retrieval. However, the corresponding Top-1 changes are smaller and not statistically supported in the full HiED pipeline.

The 284-case Top-3--Top-1 difference shows why candidate coverage is insufficient. In these cases, a gold parent has already entered the genuine ranked differential but is not selected as primary. The strong TF--IDF baseline reinforces this limitation of a candidate-only explanation: the same synthetic transcripts contain predictive benchmark signal that the evaluated HiED configuration does not fully use. At the same time, corpus-specific lexical cues may contribute to TF--IDF performance, so this result does not establish that every case contains clinically sufficient information.

\paragraph{Criterion verification.}

Criterion verification asks whether each configured diagnosis is compatible with the transcript under study-specific rules. It does not compare which compatible diagnosis best explains the presentation. Common features such as sleep disturbance, fatigue, low mood, worry, avoidance, and reduced concentration can therefore support several categories simultaneously. Compatibility is not comparative diagnosis.

This distinction explains the high-retention, low-selectivity pattern. A gold parent is retained in 0.937 of checker-eligible cases, while the median criterion-compatible set contains six of fourteen configured categories. The criterion-state composition of strict primary-selection failures is also close to that of correctly selected cases. Criterion verification remains useful for exposing support, contradiction, unresolved evidence, and rule application, but neither compatible-set size nor aggregate state ratios determine primacy.

\section{Primary Selection as the Main Remaining Bottleneck}

The strict computational evidence consists of 272 cases satisfying $D_3=1$, $R=1$, and $S=0$. The nearest gold parent is second in 171 cases and third in 101, and F41-to-F32 is the dominant nearest-gold-to-selected pair. These cases have passed both the ranked-candidate and criterion-retention conditions used in the analysis, leaving primary selection as the recorded location of disagreement.

This is a system-level benchmark claim rather than a clinical judgment. Some cases may reflect an incorrect primary-selection decision; others may contain insufficient transcript information, multiple plausible diagnoses, an ambiguous gold label, or an erroneous checker state. The ongoing psychiatrist case review is designed to estimate this composition. Until it is complete, the 272 cases should not be described as uniformly recoverable or clinically erroneous.

\section{Why the Evaluated Selection Strategies Did Not Close the Gap}

The evaluated strategies mostly recombine existing signals. NtS promotes diagnoses from the criterion-compatible artifacts, while deterministic fusion combines rank, compatibility, and criterion support. Pairwise comparison and debate add new inference but still operate over the same fixed transcript, and self-consistency measures repeated model preference rather than acquiring new discriminating evidence.

The results suggest that support counts and repeated preferences do not encode the comparative relations required for primacy. Relevant distinctions may include onset, temporal course, syndrome dominance, exclusions, causal direction, and whether one diagnosis better explains symptoms assigned to another. NtS and deterministic fusion reduce performance, forced pairwise override and debate are lower than DA, and the small sampling-based point estimates lack statistical support. These findings apply to the tested configurations; they do not establish that every pairwise, debate, fusion, or sampling method must fail.

\section{Set Construction, Diagnostic Scope, and External Transfer}

\paragraph{Diagnostic-set construction.}

Primary selection and multilabel set construction use different output contracts. A system may select a benchmark-consistent primary while adding an unsupported secondary diagnosis or omitting a gold comorbidity. This occurs in 109 internal cases with correct primaries but non-exact diagnostic sets.

The distinction is essential for DA--NtS interpretation. DA may emit an optional secondary diagnosis, whereas NtS emits a single diagnosis. A change in Exact Match or label-level F1 can therefore arise from output cardinality even when primary-selection accuracy decreases. Set-level agreement should not be described as calibration, and it cannot substitute for the committed Top-1 comparison.

\paragraph{Diagnostic scope.}

Configured scope determines whether a diagnosis can be emitted at all. The 47 out-of-scope MDD-5k cases therefore represent a distinct limitation from selecting among available labels. Expanding the profile successfully increases representability, but none of the 58 newly representable gold parents becomes the committed primary.

Availability in the output space is thus necessary but insufficient for behavioral adaptation. A broader profile may also require diagnostic examples, criterion definitions, comparative training, prompt adaptation, or a selector that has learned when the new categories should displace existing ones. The 0/58 result concerns the tested configuration-only expansion and is not evidence against ontology expansion as a general method.

\paragraph{External transfer.}

On the common 925-case MDD-5k trace, genuine Top-3 reaches 0.8508 while committed Top-1 reaches 0.5708, leaving 259 ranked-but-not-selected cases. NtS and deterministic fusion again decrease Top-1, while sampling-based aggregation yields small positive but unsupported point estimates. These findings are consistent with transfer of the ranking-to-selection gap and the limitations of the evaluated selection strategies.

The external evidence is incomplete. MDD-5k lacks a criterion-retention trace matched to the internal analysis, so it cannot reproduce $R$, $P(R\mid D_3)$, or $P(S\mid D_3,R)$. The restricted F32/F41 task is easier than the full multi-way task, and both corpora are synthetic. Cross-corpus TF--IDF and case-neighbor results also degrade substantially relative to within-corpus evaluation, indicating that fitted lexical and similarity signals are corpus dependent. The external results are therefore consistent with, but do not fully confirm, the internal stage-wise interpretation.

\section{Clinical Interpretation and Implications for Auditable Systems}

\paragraph{Psychiatrist case review.}

The computational strata cannot determine whether the system, the transcript, or the benchmark gold label is clinically responsible for a disagreement. The psychiatrist case-review study addresses this boundary by separating transcript-only primary choice, gold-label plausibility, information sufficiency, multiple plausible diagnoses, and review of recorded system outputs.

At the time of this manuscript revision, the study is ongoing. Partial judgments and agreement estimates are excluded to avoid selective reporting. Once complete, the review can estimate what proportion of ranked-and-retained nonselection cases are judged to be primary-selection errors and what proportion instead reflect insufficient evidence or gold-label ambiguity. It remains an expert review of synthetic transcripts rather than clinical validation.

\paragraph{System-design implications.}

Audit and selection should remain conceptually separate. HiED records ranked candidates, criterion states, linked evidence, criterion-compatible diagnoses, and finalization source. These artifacts make disagreements inspectable, but they neither reveal hidden reasoning nor establish clinical correctness. The demonstrated contribution is analytical traceability.

Future selectors require comparative evidence rather than another independent compatibility score. Structured-knowledge systems such as MedKGI and DKEC motivate relations among symptoms, diagnoses, temporal course, exclusions, and missing information~\citep{wang2025medkgi,ge2024dkec}. A diagnostic graph could represent support, contradiction, causal direction, explanatory dependence, and comorbidity, then compare which candidate explains the overall presentation with fewer unresolved conflicts. This direction remains unevaluated.

Fixed transcripts also impose an evidence boundary. Interactive systems such as MIND, WiseMind/ProAI, MAGI, and DSM5AgentFlow can ask additional questions~\citep{li2026mind,wu2026wisemind,bi2025magi,ozgun2025dsm5agentflow}, whereas fixed-record systems such as MentalSeek-Dx and MoodAngels operate on available records~\citep{sun2026mentalseek,xiao2025moodangels}. When duration, course, substance use, exclusion, or episode history is unresolved, a future system should identify the discriminating fact, request targeted follow-up information when interaction is possible, or abstain.

Finally, Taiwan is the intended setting rather than a validated population. All quantitative evidence comes from synthetic Chinese corpora. The thesis does not evaluate real Taiwanese patients, local language use, hospital workflow, clinician behavior, or patient outcomes. HiED should therefore remain a research framework for auditable analysis rather than an autonomous diagnostic tool.

Candidate detection is necessary but does not explain the large Top-3--Top-1 gap. Criterion verification retains gold labels at a high rate but evaluates compatibility rather than comparative primacy. The largest strict computational subset is consequently located at primary selection, yet its clinical composition remains unresolved until psychiatrist review is complete. The tested finalization and selection strategies do not supply a clear improvement, and diagnostic-set construction, configured scope, and corpus-specific transfer remain separate limitations. These findings support audit-oriented system design with explicit comparative evidence and bounded claims, not clinical deployment.

\chapter{Limitations and Threats to Validity}
\label{ch:limitations}

\section{Benchmark and Measurement Limitations}

The evaluation target is agreement with benchmark gold labels after parent-level projection. It is not an independent judgment of the patient's true diagnosis. Parent projection makes two corpora easier to compare but removes subtype detail. Top-1 Accuracy with a gold-label set is also less strict than matching only the first listed diagnosis. Top-1 Accuracy, Top-3 Accuracy, Exact Match, Macro-F1, and Weighted-F1 therefore measure different parts of benchmark performance; none alone measures clinical correctness or safety.

The selection bottleneck is an observed difference among output views, not a proven causal process inside the model. Under Direct-Answer, the primary diagnosis is chosen before the checker creates the audit trace. Top-1 Accuracy and Top-3 Accuracy use all 1,000 test cases, whereas gold-label retention uses 915 checker-eligible cases. These measures help locate errors but should not be read as one nested funnel. The oracle upper-bound recovery analysis is likewise an upper bound, not a deployable selector.

The strong internal TF--IDF result may reflect real transcript signal, source-corpus lexical regularities, synthetic-generator artifacts, or a mixture of all three. It narrows the interpretation of the LLM selection bottleneck but does not establish that a lexical classifier is clinically superior or transferable. Conversely, because this stronger same-split baseline exists, HiED errors alone cannot establish that primary diagnosis is intrinsically unrecoverable from a transcript.

\section{Dataset and Generalizability Limitations}

Both quantitative corpora are synthetic. They support controlled and reproducible experiments, but may contain generator-specific language, simplified ambiguity, limited comorbidity, and less noise than real practice. MDD-5k provides a useful distribution shift, but it is still synthetic and does not provide the same criterion-retention analysis as the internal set.

The official LingxiDiagBench test split was unavailable, so the main in-domain result uses a 1,000-case held-out split from the original training-source corpus. The cases were excluded from retrieval and development, but the split still shares templates and source distribution with the training data. The thesis does not report a full template-level or embedding near-duplicate audit, and foundation-model exposure to the public corpus cannot be ruled out.

The public validation split is used repeatedly for prompt, rule, sensitivity, and retrieval-configuration selection. Its results are configuration-selection evidence rather than an independent test estimate. The separate internal split reduces direct selection bias in the main estimate, but repeated use of one public split may still favor choices adapted to its corpus-specific patterns.

MDD-5k transcripts are substantially longer than the internal cases, and the external evaluation applies one deterministic prompt-budget truncation rule. This keeps the execution contract fixed, but truncation exposure and the effect of omitted middle turns are not separately quantified.

The common MDD-5k trace lacks the criterion-retention view required for the full internal $D_3$--$R$--$S$ analysis. Cross-corpus lexical and case-neighbor degradation further indicates that some fitted signals are corpus specific. The external chapter therefore tests a synthetic distribution shift rather than generalization across clinical populations.

\section{Model and Experimental-Comparison Limitations}

Not every comparison isolates one component. Single versus HiED changes several modules and measures a package effect. Common-upstream finalization methods hold recorded DA outputs fixed more closely, whereas independent pairwise reasoning, debate, self-consistency, and case-neighbor methods change calls, sampling, context, or input signals. Their results compare the tested configurations rather than establish a causal ranking of method families.

The debate comparison also differs in inference budget. The primary Direct-Answer pipeline uses an 8,192-token context window, whereas the complete advocate--judge debate requires 24,576 tokens and additional model calls. Its result therefore evaluates the full debate configuration rather than the isolated effect of debate under a matched context or compute budget.

Several analyses are exploratory or use small samples. Rare classes have wide uncertainty, and a non-significant difference does not prove equivalence. Holm correction is applied within the predefined families in Chapter~\ref{ch:experimental}, not across every analysis in the thesis. Oracle, lexical, BGE-M3, scope, and selection-intervention analyses therefore remain supporting or exploratory evidence even when an isolated result is favorable.

NtS is derived deterministically from the recorded DA ranking, criterion states, and criterion-compatible set without additional LLM calls. This isolates the specified primary-selection rule more closely than a newly generated pipeline run, but NtS emits only one label. Its Top-1 supports a paired primary-selection analysis, while Exact Match and classwise F1 conflate re-selection with output cardinality. The comparison remains an offline benchmark analysis and does not establish prospective behavior or clinical benefit.

Complete internal case-level sampling traces were not retained for SC and CISC. Those results provide weaker auditability than methods with case-level predictions and are treated as descriptive supporting evidence rather than paired selection-intervention evidence.

The primary analysis system also depends on one model family, prompt set, quantized checkpoint, serving stack, context window, and decoding setup. The Qwen3 size analysis does not establish that the same findings hold for unrelated models or runtimes.

Computational reproduction requires the datasets, exact checkpoint and quantization, retrieval index, configuration, serving environment, structured-decoding implementation, and evaluation code. Analytical reproduction additionally requires case-level predictions, label projection, endpoint definitions, and declared paired-comparison families; complete internal SC/CISC traces are a known exception. Greedy decoding reduces sampling variation, but exact identity may still depend on model revision, serving-library version, hardware, and schema recovery.

\section{Clinical and Deployment Boundaries}

An ongoing psychiatrist case-review study uses transcript-only review under the configured diagnostic scope before revealing system outputs or benchmark gold labels. Because the planned assessments and agreement analyses are incomplete, no partial comparator estimate is used in this manuscript. The present version therefore cannot determine whether a unique primary is recoverable by psychiatrists from each transcript or whether some model--gold-label disagreements reflect task ambiguity.

The benchmark gold labels and model-generated criterion states have not yet received a completed independent psychiatrist review. Criterion states are produced by an LLM checker, and errors in negation, duration, or context are propagated deterministically by the Compatibility Auditor. Reproducibility of rule application therefore does not imply validity of the underlying states, and the audit trace is neither a faithful record of hidden reasoning nor a clinician-validated criterion assessment. The computational analysis identifies failure locations rather than clinically verified diagnostic errors, and it provides no completed evidence that clinician inspection is improved by the recorded audit trace. Final reporting of the ongoing review must include reviewer selection, panel size, blinding adherence, retrospective-review bias, agreement, unresolved cases, and the applicable institutional ethics determination.

The intended setting is Taiwanese outpatient psychiatry, yet the study does not evaluate real Taiwanese patients, Traditional-Chinese clinical speech, mixed Mandarin--Taiwanese language, local coding practice, or hospital workflow. The configured diagnostic scope also maps unsupported diagnoses to \texttt{Others}. Expanding the list of legal outputs does not by itself teach the model when to use new labels, as shown by the scope-expansion analysis.

No result in this thesis establishes clinical effectiveness, clinician equivalence, treatment benefit, or safe autonomous use. There is no tested clinical abstention or unknown-diagnosis detector, calibrated confidence, subgroup fairness analysis, workflow study, patient-outcome evaluation, or measurement of automation bias. Runtime cost, per-case token use, and latency were not evaluated as formal workflow endpoints. Real-patient validation and operational evaluation must therefore precede any deployment claim.

Neither form of reproducibility establishes clinical validity. An exactly reproduced criterion state can still be clinically wrong, and a reproduced benchmark gain does not establish workflow value, safety, or patient benefit.

Future work with patient data will require institutional approval, privacy controls, access logging, monitoring, incident procedures, and periodic revalidation. Until these requirements and the clinical studies above are completed, HiED should remain a research and review prototype.

These limitations bound the thesis to reproducible synthetic benchmark evidence. The reported stage-wise gaps localize disagreement under a fixed scoring protocol, model family, and set of experimental configurations; they do not establish clinical correctness, a causal mechanism inside the model, robustness across unrelated model families, or deployment readiness.

\chapter{Conclusions and Future Work}
\label{ch:conclusion}

\section{Summary of Findings}

This thesis introduced HiED, a hybrid multi-agent framework that separates candidate detection, criterion verification, deterministic compatibility aggregation, and primary selection while recording the resulting committed primary diagnosis.

The same-split lexical baseline achieved higher Top-1 performance than HiED on the internal benchmark, showing that the synthetic corpus contains strong predictive lexical regularities. However, the lexical classifier degraded sharply under cross-corpus transfer, so its internal advantage is interpreted as corpus-local benchmark fit rather than evidence of broadly transferable diagnostic reasoning. HiED showed substantially higher ranked candidate coverage than committed Top-1 performance, and criterion checking frequently retained a gold diagnosis without determining which compatible diagnosis should be primary. Criterion-derived Nominate-then-Select finalization and the tested pairwise, debate, fusion, and sampling-based selection-intervention configurations did not provide a clear and reproducible improvement in primary selection; supporting case-neighbor analyses likewise transferred poorly. Retrieval improved the Single system, but the results did not support an advantage for parent-balanced over global retrieval or a statistically supported retrieval gain for HiED. The Top-3--Top-1 selection gap also remained visible on the external synthetic corpus.

HiED's contribution is therefore not state-of-the-art benchmark accuracy, but an auditable architecture and evaluation framework that localizes disagreement across candidate detection, criterion verification, and primary selection. The results are limited to synthetic benchmark data and do not establish clinical validity or autonomous diagnostic safety.

\section{Answers to the Research Questions}

This section summarizes how the final experiments answer the five research questions introduced in Chapter~\ref{ch:related}. Each answer is limited to benchmark performance on synthetic Chinese psychiatric transcripts and should not be interpreted as evidence of clinical effectiveness.

\begin{description}[style=nextline,leftmargin=3cm,labelwidth=2.5cm]
\item[RQ1] Under the matched internal evaluation contract, HiED did not outperform the strongest same-split baseline: TF--IDF + logistic regression achieved higher Top-1 Accuracy. This result shows that the synthetic LingxiDiag transcripts contain strong corpus-local lexical signals, but it should not be interpreted as evidence that the lexical classifier provides more transferable or clinically valid diagnostic reasoning. Its performance degraded substantially in the cross-corpus transfer experiments. HiED instead provides a ranked differential, criterion-linked evidence records, and explicit finalization artifacts, enabling candidate detection, criterion verification, and primary selection to be evaluated separately.

\item[RQ2] The largest observed discrepancy lies at primary selection after candidate detection and criterion verification. Gold diagnoses were frequently present in the genuine ranked output and retained under the configured compatibility rules, yet another diagnosis was selected as the committed primary. This supports a primary-selection bottleneck in HiED under the benchmark protocol, but it does not establish the clinically correct primary diagnosis or a universal causal bottleneck in transcript-based diagnosis.

\item[RQ3] Retrieval improved the Single system relative to no retrieval, but global and parent-balanced retrieval did not differ significantly. HiED showed no statistically supported retrieval gain across the tested strategies. Because the strategies also differed in the number and composition of examples supplied to the model, the results do not establish an independent benefit of parent balancing.

\item[RQ4] NtS reduced Top-1 Accuracy relative to Direct-Answer across all three retrieval conditions, and the complete forced pairwise-override configuration produced a substantial decrease. The tested selection rules did not improve primary selection under their respective evaluation contracts. Because NtS emits one diagnosis, its set-level metrics also reflect output cardinality.

\item[RQ5] The Top-3--Top-1 selection gap remained visible across the evaluated Qwen3 sizes and on MDD-5k, so the pattern was not confined to one model size or synthetic corpus. Lexical and case-neighbor selectors transferred poorly across corpora, and expanding the configured diagnostic scope made additional diagnoses representable but recovered none of the tested newly representable cases. Benchmark disagreements also concentrated in a small number of diagnostic pairs, especially depression--anxiety confusions. These findings support robustness only within the tested synthetic datasets, Qwen3 family, and configured analyses; they do not establish cross-model or clinical generality.
\end{description}

\section{Future Work}

\subsection{Completion and Reporting of the Psychiatrist Case-Review Study}

The ongoing psychiatrist case-review study described in Section~\ref{sec:psychiatrist-case-review} should be completed before its findings are incorporated into the substantive interpretation of the selection gap. No preliminary outcome or partial agreement estimate is used in the present conclusions. After all prespecified reviews are complete, Chapter~8 should report reviewer characteristics, the clinician-confirmed primary-selection error rate, secondary outcomes, agreement estimates, disagreement categories, and unresolved cases. The study remains an expert review of synthetic transcripts rather than clinical validation or a prospective workflow evaluation.

\subsection{Comparative Selection with Structured Diagnostic Knowledge}

The current criterion layer evaluates each diagnosis independently and therefore represents compatibility more directly than comparative primacy. A stronger selector should explicitly model relations among diagnoses, symptoms, temporal course, exclusion conditions, causal explanations, and comorbidities. One possible direction is a structured diagnostic knowledge graph in which nodes represent diagnoses, symptoms, episodes, and clinical findings, while edges represent temporal order, support, contradiction, exclusion, and explanatory dependence. The selector could then compare which candidate explains the overall presentation with the fewest unresolved conflicts, rather than selecting the diagnosis with the largest number of independently satisfied criteria. This approach should be evaluated against the current criterion-count and finalization rules under the same candidate rankings and case sets.

\subsection{Targeted Evidence Acquisition and Abstention}

Some cases may remain ambiguous because the transcript does not establish duration, onset, previous episodes, substance use, medical causes, or the relationship between concurrent syndromes. In these cases, applying another ranking rule may not be sufficient. Future systems should identify the unresolved fact that would best distinguish the leading candidates, ask a targeted follow-up question when interaction is possible, or abstain from committing to one diagnosis when the available evidence is insufficient. Conversational diagnostic systems show that LLMs can collect history~\citep{tu2025conversational}; the value of each question should be evaluated by whether it reduces diagnostic ambiguity rather than only by conversational fluency.

\subsection{Prospective Clinical Evaluation in Taiwan}

After the target labels, criterion audit, and final system configuration have been validated, HiED should be evaluated prospectively on de-identified Taiwanese psychiatric consultation transcripts. The inclusion criteria, primary and secondary outcomes, system configuration, subgroup analyses, and statistical plan should be specified before outcome analysis and publicly registered when appropriate. The evaluation should distinguish the recorded clinical diagnosis, transcript-only psychiatrist review, and fuller clinical assessment. Subsequent studies should examine workflow value, subgroup performance, automation bias, privacy, monitoring, and patient safety before any deployment claim is made.

\bigskip
\noindent A system may rank a benchmark-supported diagnosis as plausible without selecting it as the committed primary. HiED makes this distinction observable by separating candidate detection, criterion verification, and primary selection. The next step is not simply to add more agents, but to validate the evidence representation, model comparative diagnostic relations more explicitly, and evaluate the resulting system on real Taiwanese clinical data.

\appendix
\titleformat{\chapter}[display]{\raggedright\bfseries\LARGE}{Appendix \thechapter}{0.5em}{}

\chapter{Configured Diagnostic Categories and Operational Compatibility Rules}
\label{app:configured-profile}

The main HiED configuration contains fourteen ICD-10 diagnostic categories. Each category has a corresponding Criterion Checker and a deterministic operational compatibility rule. Subtype-level categories under F41 and F43 are evaluated under shared parent labels, but criterion checking and compatibility aggregation are performed at the configured category level. The criterion counts and thresholds below were cross-checked against the configured ontology evaluated by the rule engine.

The rules in this appendix are study-specific operationalizations informed by ICD-10 descriptions~\citep{who2019icd10}. They are not verbatim reproductions of the ICD-10 manual, have not been independently validated by clinicians, and must not be interpreted as clinical diagnostic standards. A diagnosis satisfying one of these rules is treated as compatible with the available transcript under the study configuration; this does not establish that it is clinically correct or should be selected as the primary diagnosis.

\begingroup
\small
\setlength{\tabcolsep}{3pt}
\begin{longtable}{|>{\raggedright\arraybackslash}p{2.3cm}|>{\raggedright\arraybackslash}p{1.4cm}|>{\raggedright\arraybackslash}p{3.1cm}|>{\centering\arraybackslash}p{1.5cm}|>{\raggedright\arraybackslash}p{5.1cm}|}
\caption{Configured diagnostic categories, scoring-parent projections, criterion counts, and study-specific operational compatibility rules.}
\label{tab:configured-profile}\\
\hline
\shortstack{ICD-10\\code} & \shortstack{Scoring\\parent} & Diagnostic category & \shortstack{Criterion\\count} & Operational compatibility rule\\
\hline
\endfirsthead
\hline
\shortstack{ICD-10\\code} & \shortstack{Scoring\\parent} & Diagnostic category & \shortstack{Criterion\\count} & Operational compatibility rule\\
\hline
\endhead
F20 & F20 & Schizophrenia & 8 & $\geq$1 first-rank symptom, or $\geq$2 other symptoms; duration $\geq$1 month\\
\hline
F31 & F31 & Bipolar affective disorder & 8 & $\geq$2 episodes, at least one manic\\
\hline
F32 & F32 & Depressive episode & 11 & $\geq$2 core symptoms and $\geq$4 total symptoms; duration $\geq$2 weeks\\
\hline
F39 & F39 & Unspecified mood disorder & 6 & $\geq$3 total symptoms met\\
\hline
F41.0 & F41 & Panic disorder & 5 & $\geq$3 attacks/month, $\geq$4 symptoms per attack\\
\hline
F41.1 & F41 & Generalized anxiety disorder & 5 & $\geq$4 symptoms; duration $\geq$6 months\\
\hline
F41.2 & F41 & Mixed anxiety and depressive disorder & 4 & $\geq$2 total symptoms; anxious and depressive criteria required\\
\hline
F42 & F42 & Obsessive-compulsive disorder & 4 & duration $\geq$2 weeks; distress or functional impairment required\\
\hline
F43.1 & F43 & Post-traumatic stress disorder & 4 & trauma exposure required; onset within 6 months\\
\hline
F43.2 & F43 & Adjustment disorders & 5 & onset within 1 month of stressor; duration $\leq$6 months\\
\hline
F45 & F45 & Somatoform disorders & 5 & $\geq$2 somatic symptom groups; duration $\geq$2 years\\
\hline
F51 & F51 & Nonorganic sleep disorders & 4 & $\geq$3 nights/week; duration $\geq$1 month\\
\hline
F98 & F98 & Other behavioral and emotional disorders & 6 & $\geq$3 total symptoms met\\
\hline
Z71 & Z71 & Counseling & 3 & $\geq$2 total symptoms; exclusion of a diagnosable disorder required\\
\hline
\end{longtable}
\endgroup

Subtype-level predictions under F41 and F43 are projected to their corresponding parent labels only for evaluation. Together with the nine other configured parent labels and \emph{Others}, these projections form the twelve scored classes defined in Chapter~\ref{ch:data}. Multiple diagnostic categories may satisfy their operational rules in the same case. The resulting criterion-compatible set therefore represents compatibility, not a mutually exclusive diagnosis or a psychiatrist-reviewed clinical primary.

\chapter{Detailed Experimental Statistics and Auxiliary Configurations}
\label{app:supporting}

\section{Auxiliary Primary-Selection Configurations}

Table~\ref{tab:additional-inference-configs} records settings for auxiliary primary-selection analyses that are not part of the core architecture defined in Chapter~\ref{ch:architecture}. These methods do not all share the same inference path or computational budget. Their results are therefore interpreted as evaluations of the complete stated configurations rather than as one common component ablation.

\begingroup
\small
\begin{longtable}{|>{\raggedright\arraybackslash}p{3.2cm}|>{\raggedright\arraybackslash}p{6.0cm}|>{\raggedright\arraybackslash}p{5.0cm}|}
\caption{Auxiliary primary-selection configurations used in supporting analyses.}
\label{tab:additional-inference-configs}\\
\hline
Analysis & Configuration & Interpretation boundary\\
\hline
\endfirsthead
\hline
Analysis & Configuration & Interpretation boundary\\
\hline
\endhead
Independent pairwise comparison & Re-executes the HiED pipeline with the pairwise differential stage enabled after criterion checking; eligible candidates require a met ratio of at least 0.50 and enter comparison when an adjacent support gap is below 0.10 & Regenerates upstream outputs and is interpreted as a complete-configuration comparison\\
\hline
Forced pairwise override & Full pairwise-commit pipeline with mandatory promotion of a valid in-cluster preference; genuine ranking may differ from DA & Complete-configuration sensitivity analysis, not an isolated finalizer or deployable fallback policy\\
\hline
Two-view debate & At most two advocate rounds followed by a judge when required; 24,576-token context window & Uses additional calls and a larger context budget than DA\\
\hline
Self-consistency & $K=3$ and $K=5$ Diagnostician samples at temperature 0.7 under fixed seed schedules; primary selected by majority vote & Samples only the Diagnostician and does not reproduce the full HiED pipeline\\
\hline
Confidence-informed self-consistency & Uses the same samples but weights each primary vote by verbalized confidence clipped to $[0,1]$; missing confidence receives weight 1 & Verbalized confidence is not a calibrated probability\\
\hline
Deterministic fusion & Validation-selected rule combining Diagnostician rank, compatibility status, and criterion met ratio; overrides only when the DA primary is not criterion-compatible and a criterion-compatible Top-3 alternative exceeds its ratio by at least 0.20 & Evaluates one selected deterministic rule on held-out cases\\
\hline
\end{longtable}
\endgroup

\section{Detailed Paired-Comparison Statistics}

Tables~\ref{tab:internal-paired-tests} and~\ref{tab:external-paired-tests} retain the discordant correctness counts and $p$-values omitted from the main result tables. In each row, ``A only'' counts cases correct under configuration A but not B; ``B only'' counts cases correct under B but not A. These correspond to correct-to-wrong and wrong-to-correct changes, respectively, when A is replaced by B.

\noindent\begin{minipage}{\textwidth}
For the retrieval-by-finalization analysis, the difference-in-differences contrasts are global minus parent-balanced, $-0.1$ percentage points (95\% CI [$-1.3$, 1.0]); no retrieval minus parent-balanced, 0.0 (95\% CI [$-1.2$, 1.2]); and global minus no retrieval, $-0.1$ (95\% CI [$-1.1$, 0.9]).
\end{minipage}

\begin{table}[H]
\centering
\caption{Detailed internal paired-test results. Reported adjustments reproduce the prespecified comparison-family analyses; rows marked exact are unadjusted two-sided exact McNemar tests.}
\label{tab:internal-paired-tests}
\small
\begin{tabularx}{\textwidth}{|>{\raggedright\arraybackslash}X|r|r|p{2.7cm}|p{2.8cm}|}
\hline
Comparison (A versus B) & A only & B only & Reported $p$-value & Test status\\
\hline
HiED--DA global versus parent-balanced retrieval (validation) & 29 & 27 & 0.894 & Exact\\
TF--IDF + LR versus HiED--DA & 229 & 115 & $3.14\times10^{-9}$ & Holm-adjusted\\
DA versus NtS, no retrieval & 37 & 9 & $<0.001$ & Holm-adjusted\\
DA versus NtS, global Top-5 & 44 & 15 & $<0.001$ & Holm-adjusted\\
DA versus NtS, parent-balanced & 45 & 17 & $<0.001$ & Holm-adjusted\\
DA versus forced pairwise override & 107 & 32 & $1.21\times10^{-10}$ & Exact\\
DA versus independent pairwise & 30 & 36 & 0.539 & Exact\\
DA versus two-view debate & 49 & 16 & $5.08\times10^{-5}$ & Exact\\
\hline
\end{tabularx}
\end{table}

\begin{table}[H]
\centering
\caption{Detailed external paired-test results on the common 925-case MDD-5k trace. All $p$-values are unadjusted two-sided exact McNemar tests.}
\label{tab:external-paired-tests}
\small
\begin{tabularx}{\textwidth}{|>{\raggedright\arraybackslash}X|r|r|p{3.0cm}|}
\hline
Comparison (A versus B) & A only & B only & Exact $p$-value\\
\hline
DA versus NtS & 190 & 38 & $1.7\times10^{-25}$\\
DA versus deterministic fusion & 122 & 30 & $2.3\times10^{-14}$\\
DA versus two-view debate & 230 & 52 & $7.0\times10^{-28}$\\
DA versus self-consistency ($K=3$) & 27 & 34 & 0.44\\
DA versus self-consistency ($K=5$) & 23 & 33 & 0.23\\
DA versus confidence-informed SC ($K=3$) & 27 & 34 & 0.44\\
DA versus confidence-informed SC ($K=5$) & 23 & 33 & 0.23\\
\hline
\end{tabularx}
\end{table}

\section{Exact Model-Scale Results}

\begin{table}[htbp]
\centering
\caption{Qwen3 model-scale results on the same 1,000 internal cases.}
\label{tab:qwen-size-results}
\begin{adjustbox}{max width=\textwidth}
\begin{tabular}{|c|c|c|c|l|}
\hline
Size & Single Top-1 & HiED--DA Top-1 & HiED Top-3 & Recorded output issue\\
\hline
4B & 0.143 & 0.538 & 0.807 & 17 Single abstentions; 164 HiED recoveries\\
8B & 0.175 & 0.520 & 0.818 & None\\
14B & 0.391 & 0.497 & 0.803 & None\\
32B & 0.465 & 0.518 & 0.803 & None\\
\hline
\end{tabular}
\end{adjustbox}
\end{table}

\renewcommand{\bibname}{References} % IEEE 慣例與 CCU「參考文獻」對齊
\clearpage\phantomsection
\addcontentsline{toc}{chapter}{References}
\bibliographystyle{IEEEtranN}
\bibliography{references}
\end{document}
